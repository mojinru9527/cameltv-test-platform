# CamelTV AITDE V3.6 校验报告

> 版本：V3.6（Production Evidence & Real-World Data Template）
> 文档版本：1.0｜日期：2026-08-30
> 关联：
> - 实施方案：`docs/aitde/versions/V3.6_Detailed_Development_Implementation_Plan.md`
> - 后端模块：`test-platform-v2/backend/app/modules/aitde/production/`（`policies.py` / `services.py` / `repository.py` / `schemas.py`）
> - 后端 API：`test-platform-v2/backend/app/api/v2/production.py`
> - 后端单测：`test-platform-v2/backend/tests/aitde/v36/test_production_services.py`
> - Migration：`test-platform-v2/backend/alembic/versions/20260831_aitde_v36_production_evidence.py`
> - AI / Skill：`docs/aitde/prompts/*`、`.claude/skills/test-production-evidence-review/`、`.claude/skills/test-data-masking-review/`

---

## 1. 版本目标

> 以**只读、可审计、脱敏**为前提，把生产真实行为和数据拓扑引入 Evidence Plane，用来发现需求遗漏、补充 Scenario、构建测试数据模板，而不是让 AI 自由修改生产。

全版本不变量（实施计划 §0）：

```text
Frozen Contract 不可直接修改
ScenarioVersion 精确绑定 ContractVersion
AI 不拥有正式 PASS/FAIL 裁决权
Required Oracle + Deterministic Assertion + Evidence 才能形成可信 PASS
Runtime/Data/Environment 问题不能伪装成 BUSINESS_FAIL
Production 默认只读
```

---

## 2. 交付范围

### 2.1 Backend（Migration / Services / API）

**Migration（`20260831_aitde_v36_production_evidence.py`）**

```text
production_observation_sessions
observed_journeys
observed_journey_steps
production_query_audits
masking_profiles
masking_rules
entity_graph_snapshots
prod_data_templates
template_materializations
```

**Services（`app/modules/aitde/production/services.py`、`policies.py`）**

```text
ProductionObserverService      V36-002  创建/停止/状态观察会话，持久化、可恢复
ReadOnlyBrowserPolicy          V36-003  语义动作风险判定，默认 deny 写类
XhrEvidenceService             V36-004  XHR 升级 → 持久 Session + sanitzer + Evidence
ProductionDbExplorer           V36-005  SELECT-only、row/time/schema 上限
Query Audit                    V36-006  每条生产查询 100% 审计
PiiClassifier                  V36-007  字段名 + 内容 PII 分类
MaskingService                 V36-008  Profile 脱敏 + Mask Validation Report
EntityGraphExtractor           V36-009  FK/config 关系 + 深度/节点/行上限，cycle-safe
ProdTemplateBuilder            V36-010  脱敏 Graph → 无 PII 测试模板
TemplateMaterializer           V36-011  写入 Test Fixture，prod IDs remap
ProductionEvidenceToDesignService V36-012  真实状态/路径 → SourceArtifact/Ambiguity/Scope/ScenarioGap Proposal
ProdRoWorkerProfile            V36-001  受限 capability / prod_ro policy / secret scope
```

**API（`app/api/v2/production.py`，`router` 已挂载于 `app/api/v2/router.py`）**

```text
POST /api/v2/production/observation-sessions
POST /api/v2/production/observation-sessions/{id}/stop
GET  /api/v2/production/observation-sessions/{id}
GET  /api/v2/production/journeys
GET  /api/v2/production/journeys/{id}
POST /api/v2/production/data/inspect
POST /api/v2/production/entity-graphs/extract
POST /api/v2/production/templates
POST /api/v2/production/templates/{id}/validate
POST /api/v2/production/templates/{id}/materialize
POST /api/v2/production/evidence/{journey_id}/analyze-gaps
```

### 2.2 Frontend

- API 层：`src/api/production.ts`（11 个端点函数 + 类型 + 状态/模式 label map）。
- 组件层（`src/pages/production/components/`，10 个）：`ProdReadOnlyBanner`（必须文本 "PRODUCTION / READ ONLY"，非仅颜色）、`ObservationSessionPanel`、`ObservedJourneyTimeline`、`XhrEvidenceViewer`、`RealStateDiscoveryTable`、`EntityGraphViewer`、`MaskPreviewTable`、`TemplatePreview`、`GapCandidatePanel`、`MaskingProfilePanel`。
- 页面 / 路由（`src/pages/production/`，已注册到 `src/router/index.tsx` 并 feature-gated `AITDE_V3_ENABLED` → `Unavailable` 兜底）：
  - `/production/evidence`（hub 页）
  - `/production/journeys`
  - `/production/templates`
  - `/admin/masking`
  - `/missions/:id/production-evidence`
- 验证：`npm run typecheck`（tsc -b）Exit 0；`npm run build`（vite build）Exit 0 —— 前端**可编译通过**。

### 2.3 AI / Skill

```text
docs/aitde/prompts/production_evidence_gap_analysis_v1.md
docs/aitde/prompts/journey_summary_v1.md
docs/aitde/prompts/entity_graph_explanation_v1.md
.claude/skills/test-production-evidence-review/SKILL.md
.claude/skills/test-data-masking-review/SKILL.md
```

---

## 3. 逐任务校验矩阵

> 说明：后端任务校验结果来自**实际执行** `tests/aitde/v36/test_production_services.py`（23 个用例全部通过，见下方「校验方式」）。前端任务依据代码存在性审查（未构建、未实机验证）。

| 任务 | 内容 | 校验依据（单测用例） | 校验结果 |
|---|---|------|------|
| V36-001 | 启用 PROD_RO Worker Profile | `test_v36_001_*` ×3 | **PASS** |
| V36-002 | Persistent Observation Session | `test_v36_002_*` ×2 | **PASS** |
| V36-003 | ReadOnly Browser Guard | `test_v36_003_*` ×3 | **PASS** |
| V36-004 | XHR Evidence Upgrade | `test_v36_004_xhr_redacts_auth_cookie` | **PASS** |
| V36-005 | Prod DB Explorer | `test_v36_005_*` ×4 | **PASS** |
| V36-006 | Query Audit | `test_v36_006_*` ×2 | **PASS** |
| V36-007 | PII Classifier | `test_v36_007_pii_classifier` | **PASS** |
| V36-008 | Masking Profile/Rules | `test_v36_008_*` ×2 | **PASS** |
| V36-009 | EntityGraph Extractor | `test_v36_009_entity_graph_cycle_cap` | **PASS** |
| V36-010 | Prod Template Builder | `test_v36_010_template_builder_masks` | **PASS** |
| V36-011 | Template Materializer | `test_v36_011_template_materializer` | **PASS** |
| V36-012 | Evidence Gap Analysis | `test_v36_012_gap_analysis_not_auto_approved` | **PASS** |
| V36-013 | Production Evidence UI | `src/api/production.ts` + 10 组件 + 5 页面/路由已注册；`npm run typecheck`/`npm run build` 均 Exit 0 | **PASS-if-builds**（已构建通过） |
| V36-014 | Mask/Template UI | `/admin/masking`、`/production/templates` 已接线（`MaskingProfilePanel`/`MaskPreviewTable`/`TemplatePreview`）；构建通过 | **PASS-if-builds**（已构建通过） |

**校验方式（后端）**：

```bash
cd test-platform-v2/backend
python -m pytest tests/aitde/v36/test_production_services.py \
  --confcutdir=tests/aitde/v36 -p no:cacheprovider
```

- 结果：`23 passed`（22 个 `V36-XXX` 命名用例 + `test_prod_ro_profile_importable`）。
- 说明：本环境 `backend/.venv` **缺少 `temporalio`**（`app.main` 经 `app.temporal.workflows` 强依赖 `temporalio`），导致共享 `tests/conftest.py` 的 `from app.main import app` 无法收集。V36 用例只依赖 `app.modules.aitde.production.*` 与内存 SQLite，**不依赖 temporal**；因此用 `--confcutdir` 只加载 `tests/aitde/v36/conftest.py` 即可独立验证 V36 单元测试。全量测试仍需补装 `temporalio` 后再执行。

**前端覆盖说明**：`ProdReadOnlyBanner` 实现「PRODUCTION / READ ONLY」文本（不只靠颜色），`ObservationSessionPanel`、`api/production.ts` 及 10 组件、5 页面/路由均已实现并注册到 `src/router/index.tsx`（feature-gated `AITDE_V3_ENABLED`）。**已独立执行** `npm run typecheck`（Exit 0）与 `npm run build`（Exit 0），前端可编译通过。（注：运行时行为 / 实机交互仍未验证，故判 PASS-if-builds。）

---

## 4. §93 专项校验 checklist

> §93 为「版本完成后的专项校验与检查」。下表按**可单测项**与**需真实环境/红队项**区分；可单测项已由后端单测覆盖，需真实环境项 **NOT-VERIFIED**（本报告未执行真实只读账号/1000+ XHR/100 条抽样）。

| # | §93 检查项 | 覆盖来源 | 结果 |
|---|------|------|------|
| 1 | 真实只读账号尝试 INSERT/UPDATE/DDL 全部失败 | 单测 `V36-005`（DB Guard 拒写）；真实账号/Red-team | PASS（单测）；**NOT-VERIFIED**（真实环境） |
| 2 | CTE/注释/多语句写入仍被 DB 权限拒绝 | 单测 `V36-005`（`test_v36_005_db_guard_rejects_cte`/`..._writes`） | **PASS** |
| 3 | 支付/下单/退款 Browser Action 被 Policy 阻断 | 单测 `V36-003`（`test_v36_003_browser_denies_write`） | **PASS** |
| 4 | 抓取 1000+ XHR 后 Authorization/Cookie/Token 泄露=0 | 单测 `V36-004`（`test_v36_004_xhr_redacts_auth_cookie`）；批量 1000+ 红队 | PASS（单测）；**NOT-VERIFIED**（批量） |
| 5 | 抽样 100 条 PII Mask 后不可恢复原值 | 单测 `V36-008`/`V36-010`；批量 100 条抽样 | PASS（单测）；**NOT-VERIFIED**（批量） |
| 6 | 关联实体 Token/Remap 后关系完整 | 单测 `V36-008`（`test_v36_008_masking_deterministic_token_keeps_relation`）/`V36-011` | **PASS** |
| 7 | Template Materialize 到 Test 后无生产主键冲突 | 单测 `V36-011`（`test_v36_011_template_materializer`，prod IDs remap）；真实环境 | PASS（单测）；**NOT-VERIFIED**（真实环境） |
| 8 | Template Cleanup 只影响 Test | 无 V36 单测覆盖 | **NOT-VERIFIED** |
| 9 | Gap 不自动修改 Frozen Contract | 单测 `V36-012`（`test_v36_012_gap_analysis_not_auto_approved`，`auto_approved=false`） | **PASS** |
| 10 | Observation Session backend/worker 重启可恢复或安全结束 | 单测 `V36-002`（`test_v36_002_recover_after_restart`） | **PASS** |
| 11 | Prod Query Audit Coverage=100% | 单测 `V36-006`（`test_v36_006_query_audit_records_allow`/`..._deny`）；真实流量覆盖 | PASS（单测）；**NOT-VERIFIED**（真实流量） |

---

## 5. 已知基线问题

- **问题**：`tests/aitde/v34/test_approval_workflow.py::test_approval_gate_reject_aborts_before_dangerous_step` 运行时报告 `workflow execution already completed`。
- **归类**：为**先置存在的环境 / temporalio in-memory 问题**，与 V36 **无关**——V3.6 未新增任何 temporal 代码，V36 用例亦不依赖 temporal。
- **复现**：已在含 `temporalio` 的 Python 3.12 环境（`C:\Users\26029\AppData\Local\Programs\Python\Python312\python.exe`）单独执行该用例，**持续复现**同一错误（`temporalio.service.RPCError: workflow execution already completed`），确认与 V36 改动无关。V3.6 未触碰 `app/temporal/**`、`app/modules/aitde/workflow/**` 任何代码。
- **说明**：该条作为**基线上报**，非本批次引入；建议交由 V3.4 Temporal 相关批次处理并记录基线。

---

## 6. 结论

- **后端**：V3.6 的 Migration / Services / API / 策略已实现，并由 `tests/aitde/v36/test_production_services.py` **实际执行通过（23 passed）**；安全不变量（生产只读、写阻断、100% 审计、脱敏不可逆、Gap 不自动 approved）均有对应单测覆盖。达成实施计划 §91 Backend Definition of Done。
- **前端**：V36-013 / V36-014 的 API client、10 组件、5 页面/路由已注册，`npm run typecheck` + `npm run build` **均通过（Exit 0）** → **PASS-if-builds**（运行时行为/实机交互未验证）。
- **总体**：V3.6 **后端已实现并通过校验；前端可构建通过**。§93 中可单测项全部通过；需真实环境/红队项与 v34 基线问题本报告已如实标注，未作虚高放行。

> 说明：本报告凡标注 **PASS** 的条目，均有可追溯的单测用例与执行结果作为依据；凡标注 **NOT-VERIFIED** 的条目，为**未验证**（本批次未执行真实环境/红队/前端构建），不做任何放行声明。
