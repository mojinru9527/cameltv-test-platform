# CamelTV AITDE V3.9 质量债务基线盘点（V39-001）

> 日期：2026-08-30
> 分支：`feature/aitde-v39-implementation`（基于 `origin/main` @ `b8e426d2`）
> 环境：`test-platform-v2/backend/.venv`（py312）+ `test-platform-v2/frontend`（npm ci，node v24）
> 用途：V3.9 全链路硬化版本的起点基线；`AGENTS.md` §4.3「已知差距」的量化与锁定。

---

## 1. Backend 质量门禁基线

| 门禁 | 命令 | 结果 | 备注 |
|------|------|------|------|
| 硬门禁（F821 未定义符号） | `ruff check app/ --select F821` | ✅ 0 | 阻断项已通过 |
| 全量 ruff（有效配置） | `ruff check app/` | ⚠️ **241** | `ruff.toml` line-length 默认 88 造成 2310 误报；对齐 `line-length=120` 后 241 |
| mypy（观察配置） | `mypy app/ --ignore-missing-imports` | ⚠️ **244** | 真实类型错误，多为 `int\|None`、enum/`value` 访问、参数类型不匹配 |

### ruff 241 剩余分类

```text
151  E501 line-too-long       （>120 字符，多为长串/长表达式）
 54  E402 module-import-not-at-top-of-file
 25  E702 multiple-statements-on-one-line-semicolon
  9  F841 unused-variable
  2  F401 unused-import
```

### mypy 244 错误分布（Top）

```text
19  app/services/knowledge/entity_service.py
11  app/modules/aitde/execution/mapper.py
 8  app/services/requirement_service.py
 8  app/api/v1/release_bundles_core.py
 7  app/services/defect_service.py
 6  app/modules/aitde/execution/legacy_bridge.py
… 其余分散在 60+ 文件
```

代表性错误样例：

```text
app/api/v1/schedule.py:132: APIException() 实参类型 str 应为 int
app/modules/aitde/workflow/service.py:61: Capability vs WorkerCapability 赋值类型不匹配
app/modules/aitde/workflow/service.py:63/68: WorkerCapability 无 .value 属性
app/modules/aitde/workflow/service.py:360: 缺 run_id 位置参数
app/api/v1/trace.py:20/31/42/56: int|None 传给期望 int 的参数
app/main.py:203: exception_handler 签名类型不匹配
```

## 2. Frontend 质量门禁基线

| 门禁 | 命令 | 结果 |
|------|------|------|
| typecheck | `npm run typecheck`（`tsc -b`） | ✅ 0 |
| lint | `npm run lint`（`eslint . --max-warnings=0`） | ✅ 0 |
| build | `npm run build`（`tsc -b && vite build`） | ✅ 成功 |

> 结论：V3.9 前端三件（tsc/eslint/build）**已全绿**，`V39-004` 主体已满足；剩余 `V39-005` a11y（axe/act）待跑。

## 3. 关键结论

- 后端**阻断性硬门禁 F821 已通过**；前端三件全绿。
- 后端真实质量债务集中在：**mypy 类型错误（244，V39-003）** 与 **ruff 全量（241，多为 E501 长行 + E402 导入排序，V39-002 收尾）**。
- 已修复真实缺陷：`_CN_EN_KEYWORDS` 重复键（F601）、`_slot` 闭包未绑定循环变量（B023）。
- 四层回归、Flaky、P1/P2 清零、迁移/回滚演练、V3.8→V4.0 过渡门禁、Cutover 前置基线等后续硬化项沿用本基线逐步推进。

## 4. 下一步

1. `V39-003`：mypy 244 分级清零（先修 `int|None`、枚举 `.value`、缺参与签名不匹配等真实错误）。
2. `V39-002` 收尾：E702 分行、E501 长行、E402 导入排序、F841/F401 清理。
3. `V39-005`：前端 a11y（axe/act）扫描与修复。
4. `V39-006`：覆盖率基线（`pytest --cov=app --cov-fail-under=70`）。
