# CamelTV AITDE V3.9 — 定向回归证据（V39-003 变更覆盖）

> 日期：2026-08-30
> 分支：`feature/aitde-v39-implementation`（基于 `origin/main` + 26+ 硬化提交）
> 说明：本沙箱无法离线跑全量回归（2124 用例因依赖服务/DB 长时间挂起），因此对 V3.9 变更所触及的后端模块执行**定向回归**（带 `--timeout`），作为本地可验证证据；全量以 CI 为准。

---

## 1. 定向回归结果（`pytest --timeout=25/30`）

| 覆盖模块（V3.9 变更点） | 测试文件 | 结果 |
|---|---|---|
| api_change_impact_service（双端点不变量） | `tests/test_api_change_impact.py` | ✅ 5 passed |
| apitest_tasks（int 收窄/参数） | `tests/test_apitest_tasks.py` | ✅ 3 passed |
| storage_retention（`_older_than` Callable 返回） | `tests/test_storage_retention.py` | ✅ 7 passed |
| ffmpeg_service（METRIC_DEFS 注解） | `tests/test_ffmpeg_service.py` | ✅ 7 passed |
| role_service / rbac（create_role 返回值收窄） | `tests/test_rbac_project_roles.py` | ✅ 5 passed |
| user_service（create_user 返回值收窄） | `tests/test_viewer_role.py` | ✅ 3 passed |
| session_credentials | `tests/test_session_credentials.py` | ✅ 6 passed |
| wiki_api / ingest / diff（E702/F401 变更） | `tests/test_wiki_api.py`, `test_wiki_ingest.py`, `test_wiki_diff.py` | ✅ 26 passed |
| requirement（F601 修复） | `tests/test_requirement.py` | ✅ 15 passed |
| api_execution（entrypoint/response） | `tests/test_api_execution_entrypoint_parity.py`, `test_api_execution_response_structure.py` | ✅ 17 passed |
| AITDE 核心（contract/mission/scenario/scope/source/ambiguity，覆盖不变量承载模块） | `tests/test_aitde_{contract,mission,scenario,scope,source,ambiguity}_service.py` | ✅ 24 passed |

**合计：118 passed，0 failed**（exit 0；含 2-3 处既有 Deprecation/Collection 警告，与本次变更无关）。

## 2. 结论
- V3.9 的后端 mypy 修复（`assert` 收窄、返回类型纠正、参数映射修正等）在覆盖到的模块上**未引入回归**（36 个用例全部通过）。
- 全量回归/覆盖率/不变量/迁移冒烟（V39-006/007/010/011）仍以 CI 为权威；本文件为本地定向回归证据。

## 3. 备注
- 变更模块未全部覆盖（如 workflow/service、temporal、entity_service、agent_orchestrator 等无对应快速单测或属集成域），其运行证据由 CI 全量回归 + 冒烟兜底。
