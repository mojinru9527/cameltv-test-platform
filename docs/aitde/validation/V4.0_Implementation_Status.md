# CamelTV AITDE V4.0 — 实施状态（实时进度）

> 分支：`feature/aitde-v40-cutover-enterprise-dev`（基于 origin/main @ `1a567595`）
> 执行器：DeepSeek_Harness（direct）
> 依据：`docs/aitde/versions/V4.0_Detailed_Development_Implementation_Plan.md` + `V3.0-V4.0_Completeness_and_Omissions_Review.md`
> 规则：每批完成后 ruff F821 + pytest（受影响模块）+ 迁移单头校验通过；外部项如实标 BLOCKED，不伪装成功。

---

## 1. 已完成（代码落地 + 校验通过）

| 批次 | 内容 | 提交 | 验证 |
|------|------|------|------|
| V40-001/002 | Legacy Usage Inventory（`legacy_usage_records`）+ Mapping/Cutover 表（`legacy_object_mappings`、`cutover_batches`）+ v2 API + 迁移 `20260904_aitde_v40_legacy_cutover` | `1a6122f3` | pytest 9、ruff、迁移单头 7 |
| V40-003 | VersionMission 写截止：`CompatibilityPolicy`（`version_mission_write_stage`），v1 create/update/delete 加门（410 Gone），读不受影响 | `3256937c` | pytest 4、ruff |
| V40-004 | TestCase Projection Cutover：`TestCaseProjectionPolicy` 绑定用例业务期望只读（409→ChangeProposal） | `d57f860d` | pytest 5、ruff |
| V40-005 | High-value 旧用例 → Scenario：`legacy_case_migrations` + `LegacyCaseMigrationService`（review 门禁、无伪装 draft、promote fail-closed）+ 迁移 `20260904_aitde_v40_legacy_case_migration` | `3f142c35` | pytest 4、迁移单头 7 |
| V40-006/007 | TestPlan→Campaign / Dataset→StaticDataSource：per-surface 写门（test-plan/dataset）+ `LegacyCutoverCompatService.resolve` 历史可读兼容 | `821a39d5` | pytest 6、ruff |
| V40-008 | v1 API 弃用策略：`V1DeprecationMiddleware`（Deprecation/Sunset/Link successor-version 头 + best-effort telemetry，注册于 app.main） | `0580f0af` | pytest 5（含 anyio 中间件测试）、ruff |

**综合**：`tests/aitde/v40` **33/33** 通过；`test_migration_single_head` 7/7；`app.main` 导入 OK；ruff F821/F401/F841/E402 全清；迁移单头保持。

> 说明：`app/services/test_case_service.py` 顶部有一处**既有** `classify_case_surface` 未用导入（F401，非本次引入，非 F821 硬门禁），未越界处理。

---

## 2. 已实现（Legacy Cutover 核心 = V40-001..008）

V40-001..008 组成了「Legacy 收敛」的完整骨架：观察旧入口（inventory）+ 建立映射（mapping/cutover batch）+ 逐事实表写截止（VersionMission / TestCase / TestPlan / Dataset）+ v1 弃用信号。后续 Wave（导航重定向、死代码移除）在 V40-019/020 前端批次处理。

---

## 3. BLOCKED（外部依赖，本会话无法离线完成）— V40-021..027

以下为 V3.9 Reality Gate 延后的 R6 认证项，需真实环境/凭据/数据集/CI，超出本沙箱可离线验证范围，**如实标记 BLOCKED**：

| 任务 | 阻断原因 |
|------|---------|
| V40-021 Smart Regression Golden | 需 ≥30 历史 ChangeSet 数据集 + 人工标注（生产历史） |
| V40-022 Replay 人工审计 | 需真实 Run 数据 + Tester 人工核对（30/30/20/10/10） |
| V40-023 XHR 红队 + PII Mask | 需真实生产/测试流量（≥1000 往返）+ canary 注入 |
| V40-024 Prod→Test Template 真实链 | 需生产/测试环境 + 账号 + 清洁模板 |
| V40-025 真实故障注入 + RED→GREEN | 需真实构建环境 + Business bug 对照 Build |
| V40-026 CI 三 job 真实启用 | 需推送触发 GitHub Actions + 真实 PG/RO secrets |
| V40-027 真实增量库 Migration Drill | 需真实 PostgreSQL 增量迁移历史（非 create_all 快照） |

> 代码层能力（`LegacyUsageInventoryService`、`LegacyCaseMigrationService`、`CompatibilityPolicy`、`LegacyCutoverCompatService`、`LegacyObjectMapping` 等）已就绪；这些任务本质是「在真实环境跑认证并留证据」，需在具备 test/prod 环境、Model/API 密钥、生产账号、历史数据与 CI 权限后再执行。

---

## 4. 已交付（Enterprise 后端 V40-009..020，除 V40-019 前端）

| 批次 | 内容 | 提交 |
|------|------|------|
| V40-011/012/014/015/017/018 | Enterprise governance 模块：`retention_policies`/`model_policies`/`model_usage_ledger`/`governance_exceptions`/`dr_test_runs` + RetentionService / ModelPolicyService / CostLedgerService / DrTestService / PlatformReadinessEvaluator + 迁移 | `9e9c5ae0` |
| V40-010/013/015 | `RbacPolicyService`（跨项目授权矩阵）+ `EncryptionVerificationService`（encryption posture knobs）+ `AcceptanceReportService`（正式验收报告） | `f45dc782` |
| V40-009/016 | `SsoService`（SSO 配置 + 外部组→本地角色映射；真实 IdP 握手 BLOCKED）+ `BackupVerificationService`（HA/backup 就绪清单 + restore drill 记录；真实演练 BLOCKED） | `6c3422fd` |

**综合**：`tests/aitde/v40` **48/48** 通过；`test_migration_single_head` 7/7；`app.main` 导入 OK；ruff F821/F401/F841/E402 全清。

> Enterprise 中需真实基础设施/凭据的项（真实 IdP 握手、真实 backup/restore/DR 演练、生产 encryption 校验）已如实 BLOCKED；达到的是可验证的代码级脚手架。

---

## 5. 已交付（V40-019 前端导航 + 治理 API）

| 批次 | 内容 | 提交 |
|------|------|------|
| V40-019 | 前端 Final Navigation：`PlatformHomeEntry` 默认落 `/missions`（mission-first）；`LegacyNoticeBanner` 旧入口只读/redirect 提示 + canonical 链；挂载于 MainLayout | `38796b3a` |
| 治理 API | `app/api/v2/governance.py`（readiness/retention/model/cost/dr/rbac/encryption/backup/sso/acceptance），供 /admin 治理页使用 | `392723bc` |

**验证**：前端 `pnpm typecheck` + `pnpm build` 均 exit 0；后端 `tests/aitde/v40` 48/48；`test_migration_single_head` 7/7；`app.main` 导入 OK；ruff 全清；工作区干净。

---

## 6. 最终状态

### 已交付（代码可验证）
- **V40-001..008 Legacy Cutover 核心**：inventory / mapping / cutover-batch / VersionMission·TestCase·TestPlan·Dataset 写截止 / v1 弃用 signal。
- **V40-009..020 Enterprise 后端**：治理表+服务、RBAC 跨项目矩阵、Encryption 校验、Acceptance Report、SSO/HA-备份脚手架 + `/api/v2/governance` 暴露。
- **V40-019 前端**：mission-first 默认导航 + legacy 只读/redirect 提示。
- 迁移：`20260904_aitde_v40_legacy_cutover`、`..._legacy_case_migration`、`..._governance`；单头保持。

### 仍待办（下一轮）
- **V40-019 治理 admin UI 页**（/admin/governance、/admin/retention、/admin/models、/admin/cost、/admin/platform-readiness、/admin/legacy-cutover、/admin/dr）——后端 API 已就绪，待前端页面消费。
- **V40-021..027（V3.9 R6 认证）** BLOCKED（真实环境/凭据/数据集/CI）。
- 真实 SSO IdP 握手、真实 backup/restore/DR 演练、生产 encryption 校验 —— BLOCKED（外部）。


