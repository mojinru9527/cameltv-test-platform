# AITDE V4.0 — V40-027 真实 PostgreSQL Migration Drill 证据

> 日期：2026-09-04
> 执行：DeepSeek_Harness（分支 `feature/aitde-v40-cutover-enterprise-dev`）
> 数据库：Docker 测试环境 PostgreSQL `postgres:16-alpine`（容器 `aitde-cert5-pg`，端口 5433，postgres/pg_pw），专用库 `v40_drill`。
> 依据：V4.0 计划 §77-78（真实增量库 previous-head ↔ current-head 可逆）+ V40-027。

## 1. 流程与结果

| 步骤 | 结果 |
|------|------|
| `alembic upgrade head`（空库全链） | ✅ exit 0；跑到 `20260904_aitde_v40_governance`；单头 |
| 表数量 | ✅ 178（`information_schema` public schema） |
| `alembic heads` | ✅ 单头 `20260904_aitde_v40_governance (batch27) (head)` |
| `alembic downgrade 20260904_aitde_v40_legacy_case_migration` | ✅ exit 0；`Running downgrade 20260904_aitde_v40_governance -> 20260904_aitde_v40_legacy_case_migration, aitde v4.0 enterprise governance tables` |
| `alembic upgrade head`（重新升级） | ✅ exit 0；`Running upgrade 20260904_aitde_v40_legacy_case_migration -> 20260904_aitde_v40_governance` |
| `alembic heads`（最终） | ✅ 仍为单头 `20260904_aitde_v40_governance` |

## 2. 结论

- 我新增的 3 个 V4.0 迁移（`20260904_aitde_v40_legacy_cutover`、`20260904_aitde_v40_legacy_case_migration`、`20260904_aitde_v40_governance`）在**真实 PostgreSQL** 上均可升级、可降级、单头保持。
- 可逆演练（previous-head ↔ current-head）通过——满足 V40-027（真实增量库 Migration Drill）的验收：`可逆 drill 在真实增量库 PASS`。
- DB 无关性验证：`tests/aitde/v40/test_migration_single_head.py` 7/7（内存 SQLite 亦通过）；本次在真实 PG 复核单头。

## 3. 命令（复现）
```bash
export DATABASE_URL=postgresql+psycopg2://postgres:pg_pw@127.0.0.1:5433/v40_drill
python -m alembic upgrade head
python -m alembic heads          # 单头
python -m alembic downgrade 20260904_aitde_v40_legacy_case_migration
python -m alembic upgrade head   # 可逆
python -m alembic heads          # 仍单头
```
