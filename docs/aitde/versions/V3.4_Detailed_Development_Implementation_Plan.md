# CamelTV AITDE V3.4 详细开发实施方案
## Temporal + Network Worker + Security Plane

> 文档版本：1.0  
> 日期：2026-08-28  
> 适用仓库：`mojinru9527/cameltv-test-platform`  
> 上一版本依赖：V3.3 VERIFIED：Driver/Command/Hybrid 幂等边界已明确  
> 下一目标版本：V3.5 Continuous Acceptance  
> 用途：直接拆 Epic / Issue / PR、数据库 Migration、后端 API / Service、前端页面、联调、测试与发布验收。

---

# 0. 版本目标

> **把 Hybrid Runtime 从单机/旧队列升级为可恢复、可重试、跨内网、带 Secret/Policy/mTLS 边界的 Durable Test Runtime。**

全版本共享不变量：

```text
Frozen Contract 不可直接修改
ScenarioVersion 精确绑定 ContractVersion
AI 不拥有正式 PASS/FAIL 裁决权
Required Oracle + Deterministic Assertion + Evidence 才能形成可信 PASS
Runtime/Data/Environment 问题不能伪装成 BUSINESS_FAIL
Production 默认只读
```

# 1. 目标架构

```text
Control Plane
  ├ FastAPI
  ├ PostgreSQL
  ├ Object Storage
  ├ Temporal
  ├ Policy Gateway
  └ Secret Metadata
       │
       ▼
Temporal Task Queues
  ├ worker-office
  ├ worker-test-a
  ├ worker-test-b
  └ worker-prod-ro（只注册基础，V3.6 才开放生产业务）
```

Worker 使用 Pull 模式；Control Plane 不主动拨入各内网。

# 2. Workflow 设计

## `ScenarioExecutionWorkflow`

```text
capture_environment_snapshot
→ plan_data
→ ensure_fixture
→ compile/resolve_command_plan
→ policy_check
→ execute_commands
→ evaluate_oracles
→ collect_evidence
→ classify_outcome
→ cleanup_fixture
→ build_replay
```

每个 Activity 定义：

```text
start_to_close_timeout
schedule_to_close_timeout
retry_policy
heartbeat_timeout（长 Browser/Data Activity）
idempotency_key
```

Cleanup 放 `finally/compensation` 路径。

## `MissionDesignWorkflow`（P1）

可逐步把 V3.0 AI Job 迁入：

```text
scope analyze
→ wait tester signal
→ ambiguity/intent
→ wait
→ contract
→ wait freeze
→ scenario
```

Human Review 通过 Signal/Update，而不是数据库轮询流程状态作为唯一控制。

# 3. Migration

## `worker_nodes`

```text
id PK
worker_key UNIQUE
name
network_zone             OFFICE|TEST|PROD_RO
status                   ONLINE|OFFLINE|DRAINING|DISABLED
version
machine_identity
tags_json
last_heartbeat_at
registered_at
```

## `worker_capabilities`

```text
id PK
worker_id
capability               HTTP|BROWSER|MYSQL|POSTGRES|LOG|KAFKA
version
config_json
UNIQUE(worker_id, capability)
```

## `workflow_runs`

```text
id PK
project_id
mission_id nullable
run_id nullable
workflow_type
temporal_namespace
temporal_workflow_id UNIQUE
temporal_run_id nullable
status
started_at
closed_at nullable
```

## `runtime_idempotency_keys`

```text
id PK
scope
key_hash
resource_type
resource_id nullable
status
created_at
expires_at nullable
UNIQUE(scope, key_hash)
```

## `policy_profiles`

```text
id PK
project_id nullable
name
policy_type
version
document_json
status
created_at
```

## `policy_bindings`

```text
id PK
project_id
environment_id nullable
network_zone nullable
policy_profile_id
priority
```

## `secret_refs`

```text
id PK
project_id
name
provider
external_ref
purpose
scope_json
status
rotated_at nullable
created_at
```

## `approval_requests`

```text
id PK
project_id
mission_id nullable
run_id nullable
action_type
request_json
status                   PENDING|APPROVED|REJECTED|EXPIRED
requested_by
approved_by nullable
created_at
resolved_at nullable
```
# 4. Worker 注册与路由

Worker 启动：

```text
Load Machine Identity
→ mTLS Authenticate
→ Register Metadata
→ Join allowed Temporal TaskQueue
→ Heartbeat Worker Registry
```

路由输入：

```text
network_zone
required capabilities
project tags
policy
```

LLM 不选择具体机器。

# 5. Secret

Secret value 只在 Worker 运行时解析：

```text
Control Plane
  secret_ref only
      ↓
Temporal Activity gets ref
      ↓
Worker local resolver
      ↓
process memory
      ↓
Driver
      ↓
discard
```

绝对禁止 Secret value 出现在：

```text
Temporal Workflow History
CommandPlan JSON
ExecutionStep input/output
Evidence
AI Prompt
普通日志
```

# 6. Policy Gateway

统一接口：

```json
{
  "actor": "user/worker",
  "project_id": 1,
  "environment_id": 5,
  "network_zone": "TEST",
  "driver": "database",
  "action": "fixture_update",
  "target": {"schema":"member_test","table":"membership"}
}
```

返回：

```text
ALLOW
DENY
REQUIRE_APPROVAL
```

所有危险 Driver 必须后端执行 Policy，不接受“前端按钮隐藏”作为安全边界。

# 7. Service / Class

- `TemporalWorkflowGateway`：start/signal/query/cancel。
- `ScenarioExecutionWorkflow`：只负责编排，不含 DB/UI 细节。
- `ExecutionActivities`：封装 Data/API/Browser/Assertion/Evidence Driver。
- `WorkerRegistryService`：注册、heartbeat、drain、disable。
- `TaskQueueRouter`：zone/capability/tag → queue。
- `SecretRefService`：只管理 metadata，不向前端返回 secret。
- `WorkerSecretResolver`：本地 resolver。
- `PolicyGateway`：OPA 或自研 Provider Adapter。
- `ApprovalService`：REQUIRE_APPROVAL 持久化，审批后 Signal Workflow。
- `IdempotencyService`：Run/Data/Cleanup 等重复调用保护。
- `LegacyQueueBridge`：把 V3 Scenario 从旧 API/UI queue 逐步迁至 Temporal。

# 8. Backend API

- `GET    /api/v2/workers` — Worker 列表
- `GET    /api/v2/workers/{id}` — 详情/能力
- `POST   /api/v2/workers/{id}/drain` — 排空
- `POST   /api/v2/workers/{id}/disable` — 禁用
- `GET    /api/v2/workflows/{id}` — Workflow 状态
- `POST   /api/v2/runs/{id}/resume` — 恢复
- `POST   /api/v2/runs/{id}/retry-step` — 受控重试
- `GET    /api/v2/approvals` — 审批列表
- `POST   /api/v2/approvals/{id}/approve` — 批准
- `POST   /api/v2/approvals/{id}/reject` — 拒绝
- `GET    /api/v2/policy-profiles` — Policy
- `POST   /api/v2/secret-refs` — 创建 Secret 引用 metadata

# 9. 旧 Queue 迁移

分步：

```text
1. Temporal 只跑新 V3 Scenario
2. API Runner 包成 Activity
3. UI Runner 包成 Activity
4. 新 V3 Run 停止写旧 queue
5. Legacy 页面仍可走旧 queue
6. V4.0 再真正退役
```

不要在本版把所有 DSH/Schedule 一次迁完。

# 10. Frontend

Admin：

```text
/admin/workers
/admin/policies
/admin/secret-refs
/admin/approvals
```

Tester Run：

```text
WAITING_WORKER
WAITING_APPROVAL
RETRYING
RESUMING
```

组件：

```text
WorkerHealthTable
WorkerCapabilityTags
NetworkZoneBadge
WorkflowProgress
RetryHistory
ApprovalGateCard
PolicyDecisionDrawer
```

普通 Tester 不看 Secret external ref、Temporal 内部细节和证书。

# 11. AI / Skill

AI 层无需理解 Temporal。

新增安全 Skill：

```text
.claude/skills/test-runtime-security-review/SKILL.md
```

审查重点：

```text
Secret 是否进入 Workflow History
Activity 是否幂等
Production Policy
Worker Zone/Capability
Retry 是否可能重复业务副作用
```

# 12. 开发任务

### V34-001｜Temporal Dev/Staging 部署

**实现内容**

- namespace/TLS/persistence/visibility

**任务级校验**

- [ ] workflow survives service restart

### V34-002｜Temporal Workflow Gateway

**实现内容**

- start/signal/query/cancel

**任务级校验**

- [ ] duplicate start idempotent

### V34-003｜ScenarioExecutionWorkflow

**实现内容**

- activity chain/compensation/finally cleanup

**任务级校验**

- [ ] worker crash resumes

### V34-004｜Execution Activity Adapters

**实现内容**

- Data/API/Browser/Assertion/Evidence

**任务级校验**

- [ ] retry no duplicate fixture

### V34-005｜Worker Registry

**实现内容**

- heartbeat/drain/version

**任务级校验**

- [ ] offline detection

### V34-006｜Capability Router

**实现内容**

- zone/capability/tag

**任务级校验**

- [ ] BROWSER 不发 HTTP-only worker

### V34-007｜mTLS Machine Identity

**实现内容**

- worker auth

**任务级校验**

- [ ] invalid cert reject

### V34-008｜SecretRef Metadata

**实现内容**

- provider/external_ref/scope

**任务级校验**

- [ ] API 不回 secret

### V34-009｜Worker Secret Resolver

**实现内容**

- runtime resolve

**任务级校验**

- [ ] Temporal History 无 secret

### V34-010｜Policy Gateway

**实现内容**

- ALLOW/DENY/APPROVAL

**任务级校验**

- [ ] internal driver 无绕过

### V34-011｜Approval Workflow

**实现内容**

- signal resume

**任务级校验**

- [ ] reject 后不执行

### V34-012｜Idempotency Store

**实现内容**

- run/data/cleanup

**任务级校验**

- [ ] duplicate activity safe

### V34-013｜Legacy API Runner Temporal Bridge

**实现内容**

- API Activity

**任务级校验**

- [ ] shadow equivalence

### V34-014｜Legacy UI Runner Temporal Bridge

**实现内容**

- Playwright Activity

**任务级校验**

- [ ] trace/evidence remains

### V34-015｜Worker Admin UI

**实现内容**

- health/capability/drain

**任务级校验**

- [ ] RBAC

### V34-016｜Run Retry/Resume UI

**实现内容**

- workflow state

**任务级校验**

- [ ] history visible

### V34-017｜Approval UI

**实现内容**

- reason/policy/diff

**任务级校验**

- [ ] approval audit    # 90. 开工准入条件

    - [ ] V3.3 Hybrid Driver 可独立调用
- [ ] Fixture/Action/Cleanup 定义幂等策略
- [ ] 测试/办公网 Worker Host 已准备
- [ ] Secret Manager/Provider 已选型
- [ ] Temporal 拓扑评审完成

    ---

    # 91. Backend Definition of Done

    - [ ] ScenarioExecutionWorkflow 使用 Temporal
- [ ] Worker Pull TaskQueue
- [ ] Capability/Zone 路由
- [ ] Secret 不进入 Control/History
- [ ] Policy 覆盖危险 Driver
- [ ] Worker crash 可恢复

    ---

    # 92. Frontend Definition of Done

    - [ ] Run 显示等待/重试/恢复
- [ ] Admin 可看 Worker
- [ ] Tester 可处理审批
- [ ] 等待 Worker 与业务失败区分

    ---

    # 93. 版本完成后的专项校验与检查

    > 开发完成后必须单独执行本节。代码合并不等于版本验证完成。

    - [ ] 执行中强杀 Browser Worker，恢复后 Run 完成且 Fixture 无重复
- [ ] 强杀 Control Plane，Workflow 可恢复查询
- [ ] 重复 Activity 不制造重复 Fixture/测试副作用
- [ ] HTTP-only Worker 不收 Browser/DB Task
- [ ] TEST Task 不错发 OFFICE-only Worker
- [ ] 无效 mTLS Worker 无法注册
- [ ] 扫描 Temporal History secret=0
- [ ] Policy DENY 后 internal 调用也不可绕过
- [ ] 审批 reject 后危险步骤不执行
- [ ] API/UI Shadow 结果一致率达门槛
- [ ] Worker drain 不收新任务
- [ ] Cleanup Activity retry 幂等

    ---

    # 94. Release Gate

    `93` 节阻断项全部通过、严重缺陷清零、迁移/回滚演练完成后，才允许把 `V3.4` 标记为 `VERIFIED`。

    人工校验项必须记录 reviewer、环境、时间和 Evidence；禁止口头放行。

    ---

    # 95. 进入下一版本前 Transition Gate

    - [ ] EnvironmentSnapshot 可在无 Git 下标识 Build
- [ ] Workflow 可由外部 Trigger 启动
- [ ] TaskQueue/Worker 长时间稳定
- [ ] Gate 所需 Run/Outcome/Evidence 可查询

    通过后冻结：Alembic Head、OpenAPI、核心枚举、不变量测试报告和本版本 Known Limitations。

    ---

    # 96. 回滚 / 降级策略

    - 关闭 DURABLE_RUNTIME 后短期退回 V3.3 coordinator
- Temporal 故障不删除 ExecutionRun
- Worker 用 drain 而非删除
- Secret metadata rollback 不删除外部 Secret

    ---

    # 97. 版本质量指标

    - Workflow Completion Rate
- Recovery Rate
- Activity Retry Count
- Worker Availability
- Queue Latency
- Policy Deny Count
- Secret Leakage=0


---

# 98. 前后端/基础设施联调顺序

```text
Round 1  Temporal Dev ↔ Workflow Gateway
Round 2  Worker Registration ↔ Worker Admin
Round 3  ScenarioExecutionWorkflow ↔ Existing Drivers
Round 4  Capability Routing ↔ Multi Worker
Round 5  SecretRef ↔ Worker Secret Resolver
Round 6  Policy / Approval ↔ Run UI
Round 7  API/UI Legacy Activity Bridge
Round 8  Crash / Retry / Resume Chaos Drill
```

# 99. 建议 PR/基础设施变更

```text
PR34-01 Temporal integration skeleton
PR34-02 ScenarioExecutionWorkflow
PR34-03 Worker Registry / Capability Router
PR34-04 mTLS / Machine Identity
PR34-05 SecretRef / Resolver
PR34-06 Policy Gateway
PR34-07 Approval Workflow
PR34-08 Idempotency
PR34-09 API/UI Activity Bridge
PR34-10 Worker/Workflow Frontend
PR34-11 Chaos / Recovery Tests
```

# 100. 建议测试文件

```text
backend/tests/aitde/v34/
  test_workflow_gateway.py
  test_execution_workflow.py
  test_idempotency.py
  test_worker_routing.py
  test_policy_gateway.py
  test_approval_workflow.py
  test_secret_redaction.py

tests/chaos/
  kill_browser_worker.py
  restart_control_plane.py
  duplicate_activity_delivery.py
```
