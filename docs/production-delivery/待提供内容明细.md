# 待提供内容明细（用户侧交付输入清单）

> **用途**：把各外部条件所需的「具体提供物」列到可执行粒度（C72-3 / C63-2）。每项提供后登记
> 提供人 / 日期 / 授权范围 / 存放位置，禁止补登假证据。未提供项对应验收保持 DEFERRED。

## 1. J15 — 外部只读页面（UI 自动化授权）

> **是什么**：平台有「UI 自动化」能力（真实浏览器跑页面）。J15 要求用**平台之外的真实页面**验证这条链路，
> 不能只测平台自己的页面。你要给的是「外部目标页面」的访问信息（只读）。
> **提供后我做什么**：拿你给的 URL 在真实浏览器打开 → 截图 + 断言 → 写入验收记录 → 关闭 J15。

| 需要提供 | 格式/示例 | 授权范围 | 存放位置 |
|---------|-----------|----------|----------|
| 目标页面 URL 列表（≥2 页） | `https://...` 每页一行 | 仅 GET/HEAD，只读，禁止登录/写入/支付/发布/压测 | 本清单 + 验收记录 |
| 访问方式 | VPN/内网说明 / 需不需要账号 | 最小权限、只读优先 | 执行器方案文档 |
| 数据范围 | 允许访问的页面/字段 | 不涉及用户信息（B4） | 本清单 |
| 脱敏要求 | 截图/日志中账号、手机号等打码 | 默认脱敏 | 验收证据 |

## 2. J16 — 音视频媒体样本

> **是什么**：平台有「音视频专项检查」（格式/帧率/音轨/健康）。J16 要求用**真实媒体文件**验证，
> 不能用平台内置假文件。你要给的是「授权使用的真实音视频文件」。
> **提供后我做什么**：跑 `/special` av-checks → 输出可复核的健康结果 → 登记 → 关闭 J16。

| 需要提供 | 格式/示例 | 授权范围 | 存放位置 |
|---------|-----------|----------|----------|
| 音视频文件样本（≥3 个） | mp4/mkv/flv 等，覆盖不同格式/时长（正常、短时长、可变帧率） | 仅测试环境分析，生产不压测 | 本机媒体目录（不入库） |
| 文件信息 | 每个文件：路径、时长、格式、大小 | — | 验收记录 |
| 授权声明 | 允许平台用于格式/帧率/音轨检测验证 | 书面/聊天确认 | 本清单 |

## 3. C58-03 — Supabase 连接验证

> **澄清**：此处的 Supabase 是**测试平台自身的托管 PostgreSQL**（Project Ref `myhwdpjmxdsodqgeecpn`，batch-58 注册），
> **不是体育平台生产环境的数据库**。体育平台生产 DB/Redis/MQ 是另一项（交付清单 §3 D1，业务内网地址）。
> 我们只验证测试平台自己的库可连接（`SELECT version()` → PostgreSQL 16），不涉及体育平台生产数据。

| 需要提供 | 格式/示例 | 授权范围 | 存放位置 |
|---------|-----------|----------|----------|
| 数据库连接串 | `postgresql://postgres.<ref>:<密码>@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres`（替换密码占位符） | 只读验证 `SELECT version()`；不写入仓库 | `test-platform-v2/config/runtime/production.env`（gitignored） |
| Project Ref 确认 | `myhwdpjmxdsodqgeecpn` 是否为预期项目 | — | 交付清单 §3 |

> **提供后我做什么**：用连接串执行 `SELECT version()` 与最小只读查询 → 关闭 C58-03。

## 4. C58-04 — production.env 回填

> **当前状态（2026-08-04 核实）**：`test-platform-v2/config/runtime/production.env` **尚未创建**。
> 已存在的 `test-platform-v2/deploy/.env`（46 个键无占位符）是本地 Compose 部署环境，**不是生产 runtime profile**。
> 需要从 `config/runtime/production.env.example` 初始化生产 profile，密码位留空由你填写。

| 键 | 需要提供 | 说明 |
|---|---------|------|
| `SECRET_KEY` | 独立强随机值 | 生产必填 |
| `ADMIN_PASSWORD` / `TESTER_PASSWORD` | 独立强随机值 | 与本地 dev 区分 |
| `DATABASE_URL` | Supabase 连接串（URL 编码密码） | 见第 3 项 |
| `ALLOWED_ORIGINS` / `CSRF_ALLOWED_ORIGINS` | `https://cameltv-test-platform1.vercel.app` | 已对齐，核对即可 |
| `AI_API_KEY` / 其他 AI 键 | 已提供（2.1） | 核对存在性 |

> **提供后我做什么**：从 example 初始化 `config/runtime/production.env` → 你填密码类值 →
> 我跑占位符扫描（0 命中）与 runtime profile 校验 → 关闭 C58-04。

## 5. C58-01 — Cloudflare

> **决策（2026-08-04 已确认）**：现有功能（Vercel 自带 HTTPS/CDN）已满足 → **暂不启用 Cloudflare**，
> 登记为豁免（C58-01 关闭）。若未来正式对外发布需要自定义域名/CDN 策略，再重新评估（对应 C72-1 后续）。

## 6. Test5 外部窗口

> **澄清**：WSL2 执行器（batch-66）**已搭建**，验收窗口（1.1，2026-08-03 11:00–18:00）**已登记**——
> 这些是「跑 Test5 的宿主与窗口」。但执行器里**还没有放入实际的运行输入**：
> OpenVPN 凭据（1.2）、内网网段/DNS（1.3）、六服务契约与只读账号（1.4）仍为 ⬜；
> 没有这些输入，Test5 任务无法真实连网执行。需要提供后才能跑通 V1~V5 验收矩阵。

| 需要提供 | 说明 |
|---------|------|
| 验收窗口（日期+时段） | 如 `2026-08-05 14:00–17:00` |
| 六服务契约 | 版本/SHA + OpenAPI/Knife4j 地址（R0-OAS-SIX-LIVE） |
| 最小权限测试账号 | 六服务各 1 个只读账号 |
| 内网网段/DNS | OpenVPN 静态路由需要（1.3） |

> **提供后我做什么**：把凭据写进 WSL2 执行器本地（不入库）→ 配 OpenVPN 静态路由 →
> 拉取六服务 OpenAPI 对比契约 → 在窗口内跑 V1~V5 验收矩阵 → 登记证据 → 关闭 Test5 相关条件。

## 7. 其他（可选，按需解锁）

| 项 | 需要提供 |
|---|---------|
| 真机性能（CP-C1/C2） | Android/iOS 真机各 ≥1 台 + App 包名 + 采集窗口授权 |
| 旧库快照（5.1） | 脱敏 PG 快照 + schema 版本说明 |
| SMTP/通知（3.x） | 测试邮箱账号密码 / 钉钉企微飞书 Webhook / Jira Token |
| ELK（3.4） | 测试端点 + 只读凭据 |

> **说明**：这些是「哪项验收被排期才需要提供」的可选项，不是一次性全要；
> 当前未排期的保持 ⬜，不影响已完成的平台交付。

## 提供方式

- 凭据类：只写 gitignored `.env` / `production.env`，禁止贴聊天明文、禁止入库（C63-2）。
- 文件类（媒体/快照）：放本机指定目录，登记 SHA-256。
- 每个提供项回填本清单「登记」列：提供人 / 日期 / 授权范围 / 存放位置。
