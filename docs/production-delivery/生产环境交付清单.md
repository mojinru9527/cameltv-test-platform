---
title: "CamelTv 测试平台 — 生产环境交付清单"
owner: "delivery-team"
created: "2026-08-02"
last_reviewed: "2026-08-02"
status: "active"
expires: "2027-02-02"
access_level: "内部交付文档 — 含内网地址，仅限授权人员访问"
tags: ["production", "delivery", "domains", "database", "infrastructure", "batch-64"]
related:
  - "../../docs/测试平台全功能验收文档-环境链接与账号汇总.md"
  - "../adr/0015-operations-release-control-plane.md"
  - "../adr/0016-three-repository-separation.md"
---

# CamelTv 测试平台 — 生产环境交付清单

> **用途**：把散落在多个文档/配置中的生产域名、服务器、数据库、账号信息收敛为单份客户交付清单。
> **安全声明**：本清单**不含任何明文密码、Token、API Key**。所有凭据以环境变量槽位表达，
> 真实值仅存在于未跟踪的 `.env` / `production.env` / Secret 管理系统中。
> **发布状态**：`DEFERRED` — production 发布门禁（服务器、制品库、Secret 管理、备份、回滚）未全部就绪，
> 不得把本清单当作 production 已发布证据（ADR-0015 §4）。

## 1. CamelTv 业务平台（被测系统）

### 1.1 生产环境（prod）

| 条目 | 值 | 备注 |
|------|-----|------|
| 用户端主站 | `https://www.camel1.tv/` | 主域名 |
| 镜像域名 1 | `https://www.camel1.to/` | -- |
| 镜像域名 2 | `https://www.camel2.live/home` | -- |
| 镜像域名 3 | `https://www.camelscore.live/` | -- |
| 镜像域名 4 | `https://www.goals365.live/en` | -- |
| 镜像域名 5 | `https://www.camellofutbol.com/es` | -- |
| API 网关 | `https://api.cameltv.live` | base_url |
| 产品模型 | 1 产品多镜像，后端服务 1 套 | 多域名均为同一产品镜像入口 |
| 访问方式 | vpn07 tun 模式 + 全局流量路由 | `vpn_required: true` |
| 当前临时状态 | `vpn_required=false`、`proxy_strategy=direct` | 待接入内网后恢复（COMMANDS.md） |
| 产品版本 | 用户端 14.1.0 / 运营后台 8.2.0 | 蓝湖「更新日志」手写版本 |

### 1.2 测试环境（test，内网）

| # | 测试节点 | 对应生产 | 站点 |
|---|---------|---------|------|
| 1 | `https://camelive-g3-test5.elelive.cn/` | `www.camel1.tv` | 主站 |
| 2 | `https://camel-to-test5.elelive.cn/id` | `www.camel1.to` | camel1.to |
| 3 | `https://camelive-g4-test5.elelive.cn/id` | `www.camel2.live/home` | camel2 |
| 4 | `https://camelive-g2-test5.elelive.cn/` | `www.camelscore.live/` | camelscore |
| 5 | `https://camel-g5-test5.elelive.cn/` | `www.goals365.live/en` | goals365 |
| 6 | `https://camel-g6-test5.elelive.cn/` | `www.camellofutbol.com/es` | camellofutbol |

Swagger/OpenAPI（内网）：Knife4j `http://camel-api-gateway05.svc.elelive.cn/camel-service/doc.html#/home`；
OpenAPI 3.0 `http://camel-api-gateway05.svc.elelive.cn/v3/api-docs`；
仓库静态副本 `test-platform/tests/api-testing/specs/cameltv-openapi.yaml`。
运营后台测试地址：`https://camel-admintest5.elelive.cn/login`（账号格式两个小写字母，验证码随意）。

## 2. 测试平台自身生产基础设施（Batch 58 注册）

| 平台 | 用途 | 关键信息 | 状态 |
|------|------|---------|------|
| Vercel | 前端托管/CDN | 项目 `cameltv-test-platform`；部署域名 `cameltv-test-platform1.vercel.app`；Framework Vite 7；Build `npm run build`；main 自动部署 | ✅ 已注册（2026-08-03 batch-68 发布演练：`/login`、`/`、`/api` 反代均 200） |
| Supabase | 托管 PostgreSQL 16 | Project Ref `myhwdpjmxdsodqgeecpn`；Region 新加坡（ap-southeast-1）；Pooler `aws-0-ap-southeast-1.pooler.supabase.com:6543`；用户 `postgres` | ✅ 已注册（密码存 production.env） |
| Cloudflare | DNS/CDN/TLS | 暂不使用（Vercel 自带 HTTPS） | ⏸️ 延后 |
| 本地 Compose 部署 | 单实例部署 | `test-platform-v2/deploy/docker-compose.yml`：postgres:16-alpine + backend + frontend(Nginx) | ✅ 模板 |

### 正式域名发布演练（batch-68/69 登记）

| 项 | 结论 | 状态 |
|----|------|:----:|
| Vercel 前端 | `https://cameltv-test-platform1.vercel.app` `/login`、`/`、`/api` 反代均 200（2026-08-03 实测） | ✅ 可访问 |
| Railway 后端 | `https://test-platform.up.railway.app/api/v1/open/health` → 200（版本 2.3.0） | ✅ 可访问 |
| ALLOWED_ORIGINS | 已与 Vercel 生产域名对齐（Railway 面板已更新） | ✅ |
| 待决策项 | ① 是否继续使用 Vercel 分配域名；② 是否启用自定义域名（Cloudflare 豁免或 DNS）；③ 正式启用公告 | ⏳ 用户决策（C68-4） |

### 正式域名发布决策材料（batch-72，C68-4）

| 选项 | 方案 | 成本/影响 | 推荐度 |
|------|------|-----------|:----:|
| A | 保持 `https://cameltv-test-platform1.vercel.app` | 零成本，立即可用；URL 含 Vercel 品牌 | ⭐ 推荐起步 |
| B | 自定义域名（如 `test.cameltv.live`）+ Cloudflare | 品牌化；需 DNS/CNAME/证书、费用与维护 | 正式对外发布时评估 |
| C | 仅内网/测试域 | 不公开；当前受限 | 不推荐 |

**建议**：先按 A 正式启用（演练已全链路 200），对外发布前再评估 B。请用户在以下三项打勾确认：① 是否按 A 启用；② 是否暂缓自定义域名；③ 启用公告文案由谁提供。

### 正式域名发布决策（batch-73 已确认）

| 项 | 决策 | 登记 |
|---|------|------|
| 发布域名 | ✅ 按 A 启用：`https://cameltv-test-platform1.vercel.app` | 用户 / 2026-08-04 |
| 自定义域名 | ✅ 暂缓（对外发布前再评估 B） | 用户 / 2026-08-04 |
| 启用公告 | ✅ 不需要公告文案 | 用户 / 2026-08-04 |
| 状态 | **正式域名发布演练+决策完成**（C68-4 / C72-1 关闭） | 2026-08-04 |

### 2.1 测试平台生产环境变量槽位（无明文值）

| 变量 | 用途 |
|------|------|
| `SECRET_KEY` | JWT 签名密钥（强随机） |
| `ADMIN_USERNAME` / `ADMIN_PASSWORD` | 超级管理员（admin） |
| `TESTER_USERNAME` / `TESTER_PASSWORD` | 测试人员（tester） |
| `DATABASE_URL` | PostgreSQL 连接串（URL 编码密码） |
| `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_DB` | Compose 内 PG 服务凭据 |
| `ALLOWED_ORIGINS` | CORS 允许来源 |
| `AI_API_KEY` / `AI_MODEL` | DeepSeek（AI 用例生成/知识向量化） |
| `LANHU_COOKIE` / `LANHU_USERNAME` / `LANHU_PASSWORD` | 蓝湖认证 |
| `SMTP_*` / 通知 Webhook | 邮件/钉钉/企微/飞书 |
| `ELK_BASE_URL` / `ELASTIC_API_KEY` | 日志检索 |
| `VPN_TUN_ADDR` | vpn07 地址 |

## 3. 服务器 / 数据库 / 中间件地址

| 类别 | 环境 | 地址 | 状态 |
|------|------|------|------|
| 业务 DB (MySQL) | test | `testdata5.elelive.cn:13356`（用户 `showmetest_app`，密码入 gitignored `.env`） | ✅ 已回填（2026-08-05 用户提供） |
| 业务 DB (MySQL) | prod | `10.x.x.x` 占位 | ⚠️ 待运维回填 |
| Redis | test | `testdata5.elelive.cn:12252`（密码入 gitignored `.env`） | ✅ 已回填（2026-08-05 用户提供） |
| Redis | prod | `10.x.x.x:6379` 占位 | ⚠️ 待运维回填 |
| RabbitMQ | test/prod | `10.x.x.x:5672` 占位（ncx 无 MQ 条目） | ⚠️ 待提供 |
| ELK/Kibana | test | `elk.elelive.cn`（索引 `cameltv-test-*`） | ✅ 内网 |
| ELK/Kibana | prod | 内网日志检索（索引 `cameltv-prod-*`） | ✅ 内网 |
| 测试平台 PG | 云 | Supabase（见 §2） | ✅ |

> 2026-08-05 澄清（C64-3）：「业务 DB/Redis/MQ」指**被测体育平台（Test5）**的生产/测试内网地址，用于交付验收连接；
> 非测试平台自身中间件。测试平台自身 DB=Supabase PG（✅），Redis/MQ 无独立实例需求。业务地址仍需运维回填。
| 测试平台 PG | 本地部署 | `postgres:5432`（Compose 内） | ✅ 模板 |
| Jenkins | 本地 | `http://localhost:8080`（agent 端口 50000） | ✅ |

## 4. 账号清单（仅公开账号名 + 槽位）

| 系统 | 账号 | 凭据来源 |
|------|------|---------|
| 测试平台 v2 | `admin` / `tester` | `.env` 注入 |
| Jenkins | `admin` / `developer` | `deploy/jenkins/.env` 注入 |
| ELK/Kibana | `eleuser` | `.env` 注入 |
| PostgreSQL (deploy) | `cameltv` | `.env` 注入 |
| Supabase | `postgres` | `production.env`（gitignored） |
| CamelTv 业务 | token 型 | `CAMELTV_{TEST,PROD}_AUTH_TOKEN` 注入 |
| 运营后台（测试） | `aa`/`bb`/… | 测试环境不校验验证码 |
| 运营后台（生产） | **不公开** | 安全策略限制（C31-3 结论） |

## 5. 网络访问条件

| 环境 | 条件 | 说明 |
|------|------|------|
| 生产 | vpn07 tun 模式 + 全局流量路由 | CI runner 必须先连 vpn07 |
| 测试 | 内网直连 | CI runner 必须在内网 |
| ELK | 内网 | test `elk.elelive.cn`；prod 内网日志检索 |

> ⚠️ 双 VPN 互斥：跑测试用 OpenVPN（不挂 vpn07）；跑生产用 vpn07（下掉 OpenVPN）。
> 双 VPN 切换手册当前暂停使用，重新启用需重新审核网络边界。

## 6. 发布前检查清单（production 门禁，ADR-0015）

- [ ] 服务器/集群、域名、TLS 就绪并登记到本清单 §3
- [ ] PostgreSQL 备份保留 + 恢复演练通过（每季度一次）
- [ ] 制品库 + release manifest + 镜像 digest 校验链路可用
- [ ] Secret Manager 或等价受控注入（禁止明文入库）
- [ ] 回滚窗口与回滚方案经审批
- [ ] 监控告警（错误率/延迟/队列/DB 连接）就绪
- [ ] test → production 同一 digest 晋级记录在案
- [ ] 客户验收材料（本清单 + 账号开通说明 + 运维手册）定稿

> 未满足以上任一条件前，production 发布状态保持 `DEFERRED`，禁止伪造发布成功证据。

## 7. 待补项（需运维/外部提供）

| 编号 | 条目 | 优先级 | 来源 |
|------|------|:------:|------|
| D1 | 业务 DB/Redis/MQ 真实内网地址与端口 | P1 | test DB/Redis 已回填（2026-08-05）；MQ 与 prod 地址待运维/用户提供 |
| D2 | 运营后台生产地址与只读账号（如政策允许） | P1 | 用户/安全 |
| D3 | `production.env` 完整回填（密码由用户本地生成） | P0 | 用户 |
| D4 | ELK `eleuser` 密码轮换（早期文档曾公开） | P2 | 安全 |

## 8. 来源文档（交叉核对）

- `docs/测试平台全功能验收文档-环境链接与账号汇总.md`
- `test-platform/config/environments/prod.yaml`、`test.yaml`、`project.yaml`
- `test-platform-v2/deploy/`（docker-compose / production-architecture / supabase-setup / cloudflare-dns-records）
- `test-platform-v2/config/runtime/production.env.example`
- ADR-0015（发布门禁）
