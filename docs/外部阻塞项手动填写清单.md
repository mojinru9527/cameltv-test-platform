# 外部阻塞项手动填写清单

> 用途：把 Batch 63 之外的外部阻塞前置条件整理为可逐项打勾的清单。
> 填写方式：在「状态」列写 ✅已提供 / ⏳待提供 / 🔒无法提供，在「填写/备注」列写
> **非秘密信息**（账号名、版本号、SHA、URL、窗口时间等）。
>
> ⚠️ 安全约定：**本清单禁止填写密码/Token/API Key/Cookie 原文**。
> 所有秘密值只写入被 Git 忽略的 `.env` / `production.env`，清单里只写「已写入 .env ✓」。

## A. Test5 与 VPN 包（B60-BLK-001 / B56-B01~B04 / B10）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| A1 | VPN 授权窗口（起止时间、联系人） | 您/网络管理员；双 VPN 手册当前已暂停，需您明确重新启用 | 窗口：__________ | 无法提供 |
| A2 | OpenVPN profile（`elevpn.ovpn` 或新 profile 逻辑名） | VPN 管理员；或您本机 OpenVPN Connect 已有配置导出 | 文件名：__________ | 无法提供 |
| A3 | vpn07 控制方式（官方 CLI/API 或仅人工开关） | vpn07 服务商/网络管理员 | 结论：__________ | 无法提供 |
| A4 | 六份服务契约当前版本/SHA（camel/live/payment/studio/konfi/account） | 各服务负责人从测试环境 Knife4j 导出（`camel-api-gateway05.svc.elelive.cn`） | camel:__ live:__ payment:__ studio:__ konfi:__ account:__ | Tester |
| A5 | 测试5 最小权限只读账号 | 业务平台/运营后台管理员创建（运营后台测试环境可用 aa/bb 组合） | 账号：__________ 权限：增加、查询、改动 |  ✅已提供 |
| A6 | 测试5 auth Token 实际值 | 用 A5 账号登录测试5 后取得 | 已写入 `.env` `CAMELTV_TEST_AUTH_TOKEN`：☐ | 这个没有固定的Auth token每次登录自动更新|
| A7 | 测试节点可用性（节点6 历史 503 是否恢复） | 运维检查 `camelive-g3-test5.elelive.cn` 等六节点 | 状态：__________ | 测试节点需要在openvpn才能启动 |
| A8 | 稳定业务数据 + 清理规则（保留期/清理 owner） | 业务数据负责人 | 规则：保留期3个月，人工清理 | ✅已提供 |

## B. AI / 蓝湖 / OCR（B60-BLK-002 / G56-011）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| B1 | DeepSeek API Key | [platform.deepseek.com](https://platform.deepseek.com) 注册 → 创建 API Key | 已写入 `.env` `AI_API_KEY`：☐ | ✅已提供 |
| B2 | Midscene 视觉模型 Key | Midscene 文档对应模型服务商控制台 | 已写入 `.env` `MIDSCENE_MODEL_API_KEY`：☐ | .env文件缺少这个模型的输入条件 |
| B3 | 蓝湖认证（Cookie 优先，或账密） | 浏览器登录蓝湖 → F12 → Application → Cookie；或提供账密 | 已写入 `.env` `LANHU_*`：☐ | ✅已提供 |
| B4 | 允许处理的真实数据范围 + 费用/隐私授权 | 产品/数据负责人书面确认 | 范围：只要不涉及用户信息 | ✅已提供 |

## C. 通知与集成端点（B60-BLK-003）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| C1 | 钉钉群机器人 Webhook 地址 | 钉钉群设置 → 智能群助手 → 添加自定义机器人 → 复制 Webhook | 已写入 `.env`：☐ | 弃用 |
| C2 | 钉钉安全模式（关键词"测试" 或 加签 Secret） | 同上，创建机器人时选择 | 模式：__________ | 弃用 |
| C3 | SMTP 邮箱授权码 | 邮箱服务商设置 → 开启 SMTP → 生成应用专用密码 | 已写入 `.env` `SMTP_*`：☐ | ✅已提供。但是smtp的发送和接收有两个地址，我不确定是哪个 |
| C4 | Jira/TAPD 域名 + API Token | 各自管理后台 → 个人设置 → API Token | 域名：__________ Token 已写入 `.env`：☐ | 弃用 |
| C5 | ELK 地址 + 密码（`KIBANA_URL`/`ELASTIC_URL`/`ELK_PASSWORD`） | 运维/日志平台管理员 | 已写入 `.env`：☐ | 暂时不需要。因为测试环境不能连接。 |

## D. 真机性能（B60-BLK-004 / CP-C1 / CP-C2）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| D1 | Android 真机（开启 USB 调试） | 设备管理员/您手头设备 | 机型/版本：__________ | 无法提供 |
| D2 | iOS 真机（iTunes/tidevice，开发者模式） | 设备管理员 | 机型/版本：__________ | 无法提供 |
| D3 | 被测 App 包名 / Bundle ID | APK 信息、应用商店页、开发侧 | 包名：__________ | 无法提供 |
| D4 | SoloX 采集环境部署地址 | 按 `test-platform-v2/docs/perf-setup.md` 部署 | 地址：__________ | 可以 |
| D5 | 采集窗口 + 恢复方案 | 性能负责人 | 窗口：__________ |  Tester|

## E. 旧 PostgreSQL 快照（B60-BLK-005 / A10）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| E1 | 脱敏旧库快照文件（pg_dump 导出并脱敏） | DBA/运维在旧生产库执行 `pg_dump` 后脱敏 | 文件名：__________ | 无法提供 |
| E2 | 来源版本号 | 旧库 `SELECT version();` / 运维记录 | 版本：__________ | 无法提供 |
| E3 | 快照 SHA-256 checksum | 导出时计算 | SHA：__________ | 无法提供 |
| E4 | 恢复步骤 + 升级前后数据断言 | DBA 提供 | 步骤摘要：__________ | 无法提供 |

## F. 云注册与后端托管（C58-01~06）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| F1 | Vercel 公开访问验证（HTTP 200） | 浏览器打开 `https://cameltv-test-platform1-aslyb72oq-test11-c8f1.vercel.app` | 状态码：_400_________ 日期：____2026-08-02______ | ✅已提供，报错ID: hnd1::cfvxk-1785651257826-c591e6c2afad |
| F2 | 本机 `production.env` 复核（0 个占位符） | 本机检查 gitignored 文件（`grep -c '<'`） | 结果：__________ | 你来检查 |
| F3 | Supabase 连接验证（`psql ... -c "SELECT version();"` → PostgreSQL 16） | Supabase Dashboard → Project Settings → Database → 连接串 | PG 版本：__________ | 你来检查 |
| F4 | Cloudflare 注册/站点（或确认用 Vercel 域名豁免 CDN） | [dash.cloudflare.com](https://dash.cloudflare.com) 注册（GitHub 登录即可） | 状态：__________ | 已经注册好 |
| F5 | 后端托管方案 + 真实公网 URL | 注册 Railway / Render / VPS 并部署 FastAPI 后端 | 平台：__________ URL：__________ |  需要提供对应的操作路径，或你帮我注册|
| F6 | `vercel.json` 反代目标回填 | 拿到 F5 URL 后由我回填 | 已回填：☐ | 不知道怎么查看 |

## G. DevOps 基础设施（OPS1 / B62-C1 / C62-C2）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| G1 | DevOps/DBA owner 登记（姓名/角色） | 您指定 | 姓名：__________ | 目前只有我一个人使用 |
| G2 | 镜像 registry（GitHub Packages / 私有 Harbor） | GitHub Settings → Developer settings → Packages；或运维 Harbor | 地址：__________ | 暂无这个账号，你直接帮我操作 |
| G3 | GitHub 自托管 runner 接入 | GitHub 仓库 → Settings → Actions → Runners → 按提示注册 | 标签/状态：__________ | 暂无这个账号，你直接帮我操作 |
| G4 | PostgreSQL 16 部署 + 备份位置/保留期 | 运维 | 地址：__________ 保留：__________ | 暂无这个账号，你直接帮我操作 |
| G5 | Secret 引用机制（Vault / .env / 其他） | 运维/安全 | 机制：__________ | 暂无这个账号，你直接帮我操作 |
| G6 | 首次执行窗口 | DevOps/发布负责人 | 窗口：__________ | 暂无这个账号，你直接帮我操作 |

## H. 平台自身 E2E 账号（C55-5-P2，可立即解锁）

| # | 需要提供 | 信息从哪获取 | 填写/备注 | 状态 |
|---|---------|-------------|----------|------|
| H1 | `E2E_USERNAME` / `E2E_PASSWORD` 指向平台自身账号 | 平台默认账号 `admin`，密码 = `.env` 的 `ADMIN_PASSWORD` | ✅已提供 |  |

## I. 已结案项（无需提供）

| 项 | 说明 |
|---|------|
| C31-3 运营后台生产地址/只读账号 | 安全策略确认不公开，验收以测试环境为准 |
| 生产固定配置 P01~P10（域名/服务器/TLS/备份窗口） | 服务器采购后由基础设施方提供，当前 DEFERRED |

## 汇总状态

| 组 | 已提供 | 待提供 | 无法提供 |
|---|:---:|:---:|:---:|
| A Test5/VPN | ☐ | ☐ | ☐ |
| B AI/蓝湖 | ☐ | ☐ | ☐ |
| C 通知/集成 | ☐ | ☐ | ☐ |
| D 真机 | ☐ | ☐ | ☐ |
| E 旧库快照 | ☐ | ☐ | ☐ |
| F 云/托管 | ☐ | ☐ | ☐ |
| G DevOps | ☐ | ☐ | ☐ |
| H E2E 账号 | ☐ | ☐ | ☐ |

> 填写完成后把本清单发回（或按组回复），我按可解锁项逐批执行验收并更新台账。

---

## 核对结果（Batch 63 QA，2026-08-02）

> 判定符号：✅ 通过 / ⚠️ 需修正或补信息 / ❌ 未达成 / 🔒 接受（无法提供）。
> 已同步：`F:\CamelTv\test-platform-v2\deploy\.env` 中的安全键已合并到
> `test-platform-v2/backend/.env`（本地运行环境，DB/ENVIRONMENT 未改动，值不回显）。

| # | 判定 | 说明 |
|---|------|------|
| A1 | ✅ | 无法提供，合理；记录为外部阻塞 |
| A2 | ✅ | 无法提供，合理；A7 依赖此项 |
| A3 | ✅ | 无法提供，合理 |
| A4 | ⚠️ | 状态"Tester"不是有效状态；六契约版本/SHA 未填 → 需服务负责人导出后补齐 |
| A5 | ⚠️ | 未填账号名；权限"增加、查询、改动"非只读 → 需补账号名，并确认只读或提供写授权+回滚方案 |
| A6 | ✅ | 理解：Token 每次登录动态更新；执行 R2 时现场登录换取，不阻塞 |
| A7 | ✅ | 备注正确（依赖 OpenVPN）；维持阻塞 |
| A8 | ✅ | 已记录：保留期 3 个月，人工清理 |
| B1 | ✅ | `.env` 中 `AI_API_KEY` 已确认非占位 |
| B2 | ⚠️ | 缺 `MIDSCENE_MODEL_API_KEY`：需 qwen-vl（通义/DashScope）Key，或确认体育 UI 自动化暂时不跑 |
| B3 | ✅ | `LANHU_COOKIE` 等已写入 `.env` |
| B4 | ✅ | 已记录：不涉及用户信息 |
| C1/C2 | ✅ | 弃用：从阻塞清单移除 |
| C3 | ⚠️ | `SMTP_HOST`/`SMTP_PORT` 值过长（疑似填了两个地址），`SMTP_FROM` 为空 → 需确认单一发件服务器/端口/发件人 |
| C4 | ✅ | 弃用：从阻塞清单移除 |
| C5 | ✅ | 记录延后：测试环境连不上 ELK |
| D1/D2/D3 | ✅ | 无法提供，合理 |
| D4 | ⚠️ | "可以"未明确是否已部署；如需要，我提供 SoloX 部署步骤 |
| D5 | ⚠️ | 状态"Tester"无效；采集窗口未填 → 待补 |
| E1~E4 | ✅ | 无法提供，合理；A10 维持阻塞 |
| F1 | ❌ | 实测 HTTP 400（错误 ID hnd1::…）→ Vercel 部署未通过，需修复（我诊断构建/运行时日志） |
| F2 | ❌ | `production.env` 未创建 → 需从 example 初始化并填真实值 |
| F3 | ❌ | 无可验证连接串（`deploy/.env` 的 DATABASE_URL 指向本地 PG）；需 Supabase 连接串填入 `production.env` |
| F4 | ✅ | Cloudflare 已注册；站点/DNS 是否启用待确认（或用 Vercel 域名豁免） |
| F5 | ⚠️ | 需要操作路径 → 我提供 Railway/Render 部署步骤（注册需您的账号） |
| F6 | ⚠️ | 解释见下：`vercel.json` 在仓库内，反代目标在 Vercel Dashboard 环境变量配置 |
| G1 | ✅ | owner 登记为您本人 |
| G2~G6 | ⚠️ | 拆分处理：我负责配置/workflow/文档；GitHub 界面操作（注册 runner、创建 Token）需您本人点按（我给精确步骤） |
| H1 | ⚠️ | 清单已勾但未落到运行环境：前端 E2E 变量为 `E2E_USERNAME`/`E2E_PASSWORD`，值=平台 `admin` + `deploy/.env` 的 `ADMIN_PASSWORD`，执行时注入即可 |

### 需要您再补的少量信息

1. A4：六份契约版本/SHA（camel/live/payment/studio/konfi/account）。
2. A5：测试5 账号名 + 确认只读（或写授权+回滚方案）。
3. B2：`MIDSCENE_MODEL_API_KEY`（qwen-vl 服务商），或确认体育 UI R2 延后。
4. C3：确认唯一发件 SMTP 主机/端口，补 `SMTP_FROM`。
5. D4：SoloX 是否已部署（是→地址；否→我出部署步骤）。
6. F2/F3：确认创建 `production.env` 并填入 Supabase 连接串（我从 example 初始化，密码位留空由您填）。
7. F5：后端托管平台选择（Railway/Render/VPS），注册后把公网 URL 给我。
8. F4：确认 Cloudflare 用 Vercel 域名豁免，或提供站点/DNS 信息。
