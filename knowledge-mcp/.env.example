# 测试平台后端地址（本地开发示例端口 8055，与 worktree 端口一致）
PLATFORM_BASE_URL=http://127.0.0.1:8055

# 平台 API Token（在平台「开放 API」页面创建，Authorization: Bearer tpat_xxx）
PLATFORM_API_TOKEN=tpat_replace_me

# 项目 ID（X-Project-Id 头）
PLATFORM_PROJECT_ID=1

# MCP 服务端口
MCP_PORT=8110
