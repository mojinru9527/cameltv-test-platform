__pycache__/
*.pyc
.venv/
venv/
.env
# SQLite runtime data dir (backend root only). Anchored so source packages named
# "data" (e.g. app/modules/aitde/data) are not excluded.
/data/
*.db
*.db-wal
*.db-shm
.pytest_cache/
.mypy_cache/
docs/openapi.json
# Batch 27 M5 — migration snapshots & POC scripts (not for repo)
backups/*.csv
scripts/poc_*
# Playwright test deps (local only, not in requirements.txt)
package.json
package-lock.json
node_modules/
playwright-tests/

# Runtime data (Batch 172: dsh sessions; also lanhu evidence default dir)
storage/
