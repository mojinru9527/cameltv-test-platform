fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.1
pyjwt[crypto]>=2.13
bcrypt>=4.1
python-multipart>=0.0.9
apscheduler>=3.10
alembic>=1.13
openpyxl>=3.1
python-docx>=1.1
httpx>=0.27
# lanhu-mcp 固定子模块运行时；后端 Provider 会直接导入该模块。
fastmcp>=2.0
beautifulsoup4>=4.12.0
# 与 backend/tests/playwright/package-lock.json 对齐；两端共用 Chromium。
playwright==1.61.0
lxml>=5.0.0
python-dotenv>=1.0.0
htmlmin2>=0.1.12
psycopg2-binary>=2.9
pyyaml>=6.0
pytest-cov>=5.0
# ── M2 RAG 向量化（本地嵌入，离线不外传）──
fastembed>=0.3            # onnx TextEmbedding（bge-small-zh-v1.5），CPU 可跑，无 torch
numpy>=1.26               # 向量存储（float32 blob）+ 暴力余弦
# AI 配置中心：Fernet 加密 AI 提供方 Key（lock 已含 cryptography==49.0.0，此处补直接依赖声明）
cryptography>=42.0
# ── AITDE V3.4 Durable Runtime：Temporal client + in-memory test WorkflowEnvironment ──
# 生产由 Control Plane worker 拉取任务；test/CI 用 temporalio.testing.WorkflowEnvironment 跑真实编排。
temporalio>=1.32
