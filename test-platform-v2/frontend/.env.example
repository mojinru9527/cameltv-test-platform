# 长期运行的 local/production 实例统一使用：
#   ../config/runtime/<target>.env
# 本文件仍用于临时单实例和独立 Agent worktree，复制为 .env.local 即可。
#
# 浏览器始终通过当前实例的同源 /api/v1 访问后端，避免跨环境 Cookie/CORS
# 混用。开发期通过 Vite proxy 转发 /api/v1 到当前实例后端，通常无需设置。
# 如需直连后端，必须保留相同的 /api/v1 契约（并确保后端 CORS 放行）：
# VITE_API_BASE=http://localhost:8000/api/v1
# 独立 worktree 由 scripts/git/new-ai-worktree.ps1 自动写入 .env.local：
# VITE_DEV_PORT=5174
# VITE_PROXY_TARGET=http://127.0.0.1:8001

# 备案号（可选）：构建前端镜像时注入，非空时页面底部展示并链接工信部备案系统。
# 生产通过 config/runtime/production.env 的 VITE_ICP_NUMBER 传入 compose build。
# VITE_ICP_NUMBER=粤ICP备XXXXXXXX号-1
