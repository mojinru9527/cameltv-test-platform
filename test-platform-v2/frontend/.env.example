# 开发期通过 vite proxy 转发 /api 到后端，通常无需改动。
# 如需直连后端可自定义（需后端 CORS 放行）：
# VITE_API_BASE=http://localhost:8000
# 独立 worktree 由 scripts/git/new-ai-worktree.ps1 自动写入 .env.local：
# VITE_DEV_PORT=5174
# VITE_PROXY_TARGET=http://127.0.0.1:8001
