# Batch 137 — Design Spec
> **Design (🎨)** | Date: 2026-08-10 | Status: 就绪

## 组件
- 证据创建表单新增"仅最新版本（如 16.0.0）"开关（Switch，与"发现并采集全部页面"同组），默认关。
- 质量报告新增 ocr_note（只读文字，用于诊断 OCR 未生效原因）。

## 状态设计
- 版本过滤：无版本文件夹 → 全量兜底；有 → 仅最新版本文件夹。
- OCR：DOM 文本存在即达标；纯图片页无文本 → 仍需 OCR/人工审核。
