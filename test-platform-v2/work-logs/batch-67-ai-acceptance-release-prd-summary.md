# Batch 67 — PRD Summary（AI 验收与正式域名发布前置条件收口）

> **Product (🟦)** | Date: 2026-08-02 | Status: Approved

## 1. 问题陈述

1. **平台 AI 验收被凭据有效性阻塞（G56-011 前置）**：用户记忆已填写 AI/蓝湖凭据，但本批实测发现
   `test-platform-v2/backend/.env` 中 `AI_API_KEY` 调用 DeepSeek `/models` 返回 **401 Unauthorized**
   （Key 格式正确但已失效/未授权）。凭据「填过」不等于「可用」，AI 验收链路仍无法解锁。
2. **正式域名发布被 DevOps 服务器阻塞（C58-06 / 6.1）**：后端托管方案未确认——Railway 上次构建因子模块失败，
   Dockerfile 已修复，但重试部署与回传公网 URL 待用户操作；无服务器则 `vercel.json` 反代目标无法回填，
   正式域名无法发布。
3. **前置条件登记不完整（C63-2）**：`docs/production-delivery/外部前置条件清单.md` 的 2.x/6.1 项状态与
   实际环境不一致（凭据已写入但未登记；6.1 无状态）。按 C63-2，解除阻塞前必须登记提供人/日期/授权范围。

## 2. 成功指标

| 指标 | 基线 | 目标 | 测量窗口 |
|------|------|------|---------|
| 2.1 AI Key 可用性 | 已填但 401 | 有效 Key 实测 `/models` HTTP 200 | 本批登记，需用户换新 Key |
| 2.2 蓝湖凭据登记 | 已填未登记 | 清单状态 ✅ + 登记字段完整（C63-2） | 本批 |
| 2.3 OCR 凭据结论 | 未登记 | 明确本地 OCR 无需云凭据并登记 | 本批 |
| 6.1 DevOps 服务器 | 未确认 | 登记部署方案与待办（Railway URL 或服务器地址） | 本批登记，需用户提供 |
| 清单一致性 | 状态滞后 | 2.x/6.1 状态与 .env 实测一致 | 本批 QA |

## 3. 非目标（本次不做）

- **不执行完整 AI 验收（G56-011）**：本批只解锁并登记前置条件；AI 全链路验收（真实设计源/OCR/正负面闭环）为
  后续批次，须等 2.1 有效 Key 落地。
- **不发布 production / 不动正式域名**：C58-06 维持 OPEN 至 6.1 提供服务器 URL。
- **不做 API-only UI（C63-1）**：继续顺延。
- **不做 V1 工具迁移（C64-1）与垃圾文件审计删除（C64-2）**：与本批无关。
- **不伪造证据**：2.1 未解锁前，清单不标 ✅（C63-2）。

## 4. 用户故事 + 验收标准

- As a 平台负责人，I want 凭据从「填过」变成「实测可用」，so that AI 验收不因假解锁返工。
  - 验收：Given 有效 Key 写入 backend/.env / When 调用 DeepSeek `/models` / Then HTTP 200 且清单 2.1 登记 ✅。
- As a 验收人员，I want DevOps 服务器方案明确，so that 正式域名发布（C58-06）可继续。
  - 验收：Given 6.1 登记 / When 用户回传 Railway URL 或服务器地址+端口 / Then 清单登记完整、`vercel.json` 反代可回填。
- As a 项目成员，I want 清单状态与事实一致，so that 后续批次不基于过期信息开工。
  - 验收：Given 本批 QA / When 交叉核对清单与 .env 实测 / Then 无状态造假（2.1 保持 ⏳ 直至鉴权通过）。

## 5. 技术考量

- 依赖：DeepSeek 控制台（platform.deepseek.com）重新生成 Key；Railway 或自建 Docker 服务器。
- 已知风险：蓝湖 Cookie 有效期（登录态过期后需重新获取）；lanhu-mcp 运行时验证留到 AI 验收批次（C67-3）。
- 安全：所有凭据只写 gitignored `.env`，清单只登记「已写入 ✓」，不回显明文（C63-2 / QA G3 密钥扫描）。

## 6. 上线计划

| 阶段 | 受众 | 成功门槛 |
|------|------|---------|
| 本批（前置条件收口） | 用户/QA | 2.2/2.3 登记 ✅；2.1/6.1 登记待办 + 解锁步骤；QA 门禁全绿 |
| 用户操作 1 | 用户 | 换新 DeepSeek Key → 实测 200 → 2.1 关闭 |
| 用户操作 2 | 用户 | Railway 重试部署回传 URL → 6.1 关闭 → C58-06 回填 |
| 后续批次 | QA | AI 验收全链路（G56-011 等）与正式域名发布演练 |

## 7. 条件对账（C-CONDITIONS.md）

- 纳入：C63-2（登记字段）、C63-3（引用 C63 条件）、C65-3（外部前置条件逐项解锁登记）、C58-06（关联 6.1）。
- 豁免：C63-1（UI 顺延）、C64-1（V1 迁移）、C64-2（垃圾文件审计）、C66-4（Test5 节点 hosts，非本批）。
- 不关闭：G56-011 / G56-012 / G56-014（AI 全链路验收，等 2.1 解锁）。
