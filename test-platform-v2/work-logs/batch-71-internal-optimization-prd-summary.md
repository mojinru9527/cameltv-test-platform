# Batch 71 — PRD Summary（内部收尾优化：C70-3/C69-3/C70-2/C65-2）

> **Product (🟦)** | Date: 2026-08-04 | Status: Approved（用户已确认执行器 Codex 并授权启动）

## 1. 问题陈述

批 68~70 交付后剩余四项内部代码/文档级收尾（无需外部授权）：

1. **C70-3（P1）**：登录限流 10 次/15 分钟硬编码（`rate_limit.py`），自动化测试/验收频繁触发 429，影响测试效率；
   需要环境化配置，生产保持安全默认，开发/测试可放宽。
2. **C69-3（P2）**：AI 分批生成串行执行 6 块耗时约 11 分钟；可限并发并行（保守 2 并发）显著提速。
3. **C70-2（P2）**：报告模板管理已交付 CRUD，但「设为默认」与章节启用配置 UI 缺失。
4. **C65-2（P2）**：过时《生产测试平台固定配置与双VPN切换验收手册》待独立审计删除。

## 2. 成功指标

| 指标 | 基线 | 目标 | 测量窗口 |
|------|------|------|---------|
| C70-3 | 硬编码 10/900 | 配置化（生产默认 10/900；开发默认放宽）；文档示例更新 | 本批 QA |
| C69-3 | 6 块串行 ~11 分钟 | 并发 2 时总耗时下降（≥30%），结果/顺序/告警语义不变 | 本批 QA（mock + 实测） |
| C70-2 | 无默认/章节 UI | 模板行内「设为默认」切换 + 章节启用勾选 | 本批 QA |
| C65-2 | 手册存在 | 独立审计删除 + 引用同步更新 | 本批 QA |

## 3. 非目标

- **C68-1/J15/J16、C68-4 域名决策、C58-01/03/04**：需用户授权/决策，保持原状态。
- **不改生产安全默认值**：限流生产 10/900 不变，仅开发/测试环境放宽。
- **不引入新依赖**：并发用 asyncio.Semaphore。

## 4. 用户故事 + 验收标准

- As a QA，I want 测试环境登录不被限流，so that 自动化用例可连续执行。
  - 验收：dev 环境 100 次/15 分钟不触发 429；prod 默认仍 10/900。
- As a 用户，I want 大文档用例生成更快，so that 等待时间可接受。
  - 验收：并发 2 后总耗时下降；结果与串行一致（顺序/去重/告警）。
- As a 测试人员，I want 模板默认与章节可配置，so that 报告格式可控。
  - 验收：行内默认切换 + 章节启用勾选持久化。
- As a 维护者，I want 过时手册被删除，so that 仓库文档保鲜。
  - 验收：文件删除、引用清理、doc-check 通过。

## 5. 条件对账

- **纳入**：C70-3、C69-3、C70-2、C65-2；C63-2/3。
- **豁免/延后**：C68-1、C68-4、C58-01/03/04、C70-1（Playground runner）。
