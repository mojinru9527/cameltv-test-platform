# Batch 82 — PRD Summary（WARN 审计一键执行器，C81-1 落地）

> **Product (🟦)** | Date: 2026-08-04 | Status: Approved

mode: light
豁免理由: 内部流程工具（一键审计执行器），不涉及产品行为/接口/配置/依赖；按 SKILL.md「批次模式」判定为轻量批次，PM/Design 工件省略，QA/Leader/看板照常。

## 1. 问题陈述

C81-1 要求"每周或每 10 批次执行 WARN 基线审计并把结果追加到趋势表"，但当前需要两条命令 + 手工编辑趋势表。需要一条命令完成：**对比基线 → 输出汇报摘要 → 自动追加趋势行**，供人工或 Codex 侧定时任务直接调用。

## 2. 成功指标

| 指标 | 基线 | 目标 | 测量 |
|------|------|------|------|
| 一键执行 | 2 命令 + 手工 | `run-warn-audit.ps1` 单命令 | 脚本运行 |
| 趋势自动追加 | 无 | 幂等追加（同日不重复） | 两次运行 |
| 汇报摘要 | 无 | AUDIT_RESULT + WARN/HARD 计数 | 脚本输出 |

## 3. 非目标（本次不做）

- **不创建 Codex 侧定时器**：本会话未暴露 automation_update 工具；脚本就绪后由用户在应用 UI 配置每周任务（收尾说明步骤）。
- **不减少 WARN 数量**：维持 C81-1 长期口径。

## 4. 用户故事 + 验收标准

- As 维护者/定时任务, I want 一条命令完成周审计, so that 趋势表自动更新且可汇报。
  - 验收：Given 仓库 / When 运行 `pwsh scripts/git/run-warn-audit.ps1` / Then 输出 AUDIT_RESULT + 计数，趋势表追加当日行；同日再跑不重复。
