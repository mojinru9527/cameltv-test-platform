# Batch 96 — PRD Summary（V1 工具审计 / 只读账号 / staging 登记 / diff 基线）

> **Product (🟦)** | Date: 2026-08-05 | Status: Review

## 0. 批次模式判定（C75-1 强制）

```markdown
mode: full
判定理由: 新增 viewer 角色/账号（新配置/行为）+ seed 变更 → 完整批次（六部门）。
```

## 1. 问题陈述

四项收尾：
- **C64-1**：V1 开发工具是否被 V2 使用需审计；用户已授权「未使用即批准废弃」；
- **C31-2/C31-3**：用户已确认生产验收结论；需创建运营只读账号供验收；
- **staging/C27-\***：无独立 staging，需登记 test 环境为替代并排期验证；
- **batch-18-C8**：diff classifier 缺标注基线，召回/误报无量化。

## 2. 成功指标

| 指标 | 基线 | 目标 |
|------|------|------|
| C64-1 | 未审计 | 11 工具引用审计 + 批准废弃记录 + 移除计划 |
| C31-3 | 无只读角色 | viewer 角色/账号（seed）+ 测试（查看 200/写 403） |
| batch-18-C8 | 无基线 | 10 组标注集 + 召回/误报指标 |
| C31-2 | Open | 用户确认 → 关闭 |
| C64-3 | 口径不明 | 交付清单澄清（业务 DB/Redis/MQ 为被测系统地址） |
| staging | 未登记 | test 环境登记为 staging 替代 + C27 排期 |
| 门禁 | — | pytest/vitest/build/scan/audit 全绿 |

## 3. 非目标

- **不实际删除 V1 工具**（本批只审计+批准+计划；删除走独立清理批）。
- **不执行 C27 四项完整验证**（数据/性能测量排期，登记 staging 替代）。
- **不改生产凭据**：viewer 密码仅由部署环境 env 提供/启动日志获取。

### C 条件纳入/豁免

| C 条件 | 处理 |
|--------|------|
| C64-1（B 档批准）、C31-2、C31-3、batch-18-C8、C64-3（澄清） | 纳入 |
| C27-C1~C4 | staging 替代登记 + 排期（执行留待数据就绪） |
| C75-1/2/3、C76-2、C78-1、C86-1 | 遵守 |

## 4. 用户故事 + 验收标准

- As a **运维/运营**, I want 一个只读账号，so that 验收时不误写数据（C31-3）。
- As a **维护者**, I want V1 工具废弃有据，so that 清理不破坏 V2（C64-1）。
- As a **质量**, I want diff 分类基线，so that 后续优化有对比（batch-18-C8）。
