# Batch 98 — QA 报告（CI 迁移 + V1 工具删除）

> **QA (🔍)** | Date: 2026-08-05 | Verdict: PASS（待 CI 双端全量 required contexts 确认后合入）

## 测试总览

| 项 | 通过 | 失败 | 阻塞 |
|:---|:----:|:----:|:----:|
| 脚本三子命令本地实测 | 3/3 | 0 | 0 |
| Python 语法编译 | ✅ | 0 | 0 |
| workflow YAML 解析 | ✅ | 0 | 0 |
| 全仓引用扫描（可执行代码） | ✅ 0 命中 | 0 | 0 |
| 仓库边界校验 | PASS | 0 | 0 |
| 条件追踪审计（-RequireLatestBatch） | exit 0（hard 0/warn 0） | 0 | 0 |
| 文档保鲜 | exit 0 | 0 | 0 |
| 通用缺陷扫描（C76-2） | HARD 0 / WARN 209（回基线，无新增类别） | 0 | 0 |

## 可执行门禁（命令 + 退出码）

| # | 门禁 | 命令 | 退出码 | 结果 |
|---|------|------|:------:|------|
| G1 | 脚本 health | `pwsh scripts/ci/api-regression.ps1 health -BaseUrls "vercel/login,railway/health"` | 0 | 两端点 200 |
| G2 | 脚本 run | `... run -BaseUrl http://127.0.0.1:9 -ReportDir temp` | 1（预期） | 6 spec / 18 用例执行，JUnit 89KB 产出，失败即非 0 |
| G3 | 脚本 collect-elk | `... collect-elk -JunitPath sample-junit.xml` | 0 | 提取 `abc123def456` 并输出 Kibana 链接 |
| G4 | Python 编译 | `python -m py_compile cli/tp.py server/main.py core/config_loader.py` | 0 | 语法通过 |
| G5 | YAML 解析 | `python -c "yaml.safe_load(...)"` 全部 workflow | 0 | 8 个 workflow 可解析 |
| G6 | 引用扫描 | `rg 'tools.<tool>|from tools|tp <cmd>' test-platform scripts .github（排除 md/generated 注释）` | 0 命中 | 无可执行引用 |
| G7 | 边界 | `python scripts/repo-split/validate_repo_boundaries.py --check` | 0 | PASS（2055 tracked 全归属） |
| G8 | 条件审计 | `pwsh scripts/git/audit-cconditions.ps1 -RequireLatestBatch` | 0 | stats Open=23 Closed=131 |
| G9 | 保鲜 | `python scripts/check_doc_freshness.py` | 0 | 更新文档元数据合规 |
| G10 | 通用缺陷扫描 | `pwsh scripts/git/scan-common-bugs.ps1` | 2（WARN 基线） | HARD 0；WARN 209 与 Batch 96 基线持平，无新增类别 |

## CI 分层核对（AGENTS.md §4.3）

- 变更范围：`.github/workflows/**` + `test-platform/**` + `repo-boundaries.json` + `C-CONDITIONS.md` + `docs/**` + `test-platform-v2/work-logs/**`
  → 按 CI 分类器为 **混合（CI + V1）**，将**保守执行双端全量**（后端 required + 前端 required 均必须跑）。
- 本批不修改 v2 backend/frontend 业务代码；双端全量回归由 PR required contexts 提供最终证据，合入前核验全绿。

## 逐项验证

- **CI 迁移**：`api-regression.yml` / `prod-smoke-test.yml` 不再出现 `tp ` 命令；token 刷新保留；JUnit 上传 artifact；失败时 collect-elk；prod smoke 移除 `--grep smoke` 空跑，实际执行 6 个只读 spec。
- **V1 删除**：11 个工具目录 21 个文件删除；`cli/tp.py` 仅保留 config；`server/main.py` 移除 envcheck/api_test/datafactory 路由；`scripts/ci-entrypoint.ps1` 删除；CLAUDE.md/README/setup/docker-compose 历史引用更新为废弃说明。
- **条件**：C64-3 关闭（prod 无法提供，验收以 test 为准）；C96-1 的 V1 删除部分关闭，C27 四项保持 Open。
- **生成式用例**：`generated by tp api pull` 头注释为历史溯源标记，非可执行引用，保留。

## 缺陷与遗留

| # | 级别 | 内容 | 处理 |
|---|:----:|------|------|
| B98-Q1 | P3 | 生成式目录无 `package-lock.json`，`npm ci` 会失败 | 脚本自动降级 `npm install`；后续可提交 lockfile 恢复 `npm ci`（排 Batch 100） |
| B98-Q2 | P3 | prod smoke 由空跑改为真实执行 6 个 spec，行为变化（有意修复） | 已登记 PRD；如个别用例在生产不稳定，按真实缺陷跟进 |
| B98-Q3 | P3 | C27 四项验证（C96-1）仍待 staging/本地全栈执行 | 保持 Open，随 C96-1 排期 |

## 发布建议

状态：**READY**（本地门禁全绿；合入前需 PR required contexts（双端全量）通过）。

## 复盘卡

| 计划耗时 | 缺陷(P0/P1/P2/P3) | 返工次数 | 根因分类 | 下次避免 |
|----------|-------------------|----------|----------|----------|
| 计划 1d / 实际 1d | 0/0/0/3 | 2（npm ci lockfile、collect-elk XML 遍历） | 工具链 | 新脚本先跑通再合入；无 lockfile 目录勿用 npm ci |

**技能使用**：`cameltv-agent-team`
