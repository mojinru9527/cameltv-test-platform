# Batch 254 — 条件追踪器重排（QA 报告 + 批次记录）

> **🔍 QA** | Date: 2026-09-18 | 分支：`fix/batch-254-conditions-restructure`（base `cc371f1e`）
> 模式：**轻量批次**（纯追踪器/文档，无代码改动）| 范围：`C-CONDITIONS.md`、`work-logs/**`

## 1. 用户决定（本批依据）

> "剩余的只需要处理『应转常驻规则、不再计待办』和『仍有效—平台内可排期』即可，其他的可以直接废弃。"

据此把 Open 段按三类重组：**常驻规则（不计待办）／真实待办（保留）／废弃（不再追踪）**。

## 2. 重排结果（`audit-cconditions.ps1` 实测）

| 项 | 重排前 | 重排后 |
|----|--------|--------|
| `stats: Open` | 56（rows=164, deferred=19） | **29（rows=132, deferred=8）** |
| `Closed` | 203（closed rows=189） | **212（closed rows=195）** |
| hard errors | 0 | **0** |

### 分类明细

| 分类 | 数量 | 内容 |
|------|------|------|
| 🟡 已被后续批次覆盖 → Closed | 9 | C244-1、C243-4、C169-1、C235-2、C235-3、C236-2、C191-3、C120-1、C120-3（均附覆盖证据） |
| ⚪ 常驻规则（新 `## 常驻规则` 节，不计统计） | 9 | C75-1、C75-2、C75-3、C76-2、C78-1、C86-1、C63-2、C63-3、C104-5 |
| 🔴 真实待办（保留在 Open） | **16** | C99-1、C106-2、C110-4、C122-2、C122-4、C123-2、C123-3、C123-4、C124-3、C161-2、C161-3、C215-1、C243-1、C246-1、C252-1、C252-2 |
| ⚫ 废弃（新 `## 废弃` 节，保留审计痕迹） | 21 | 外部阻塞/Deferred 项（C65-3、C74-2、C84-1、C95-1、C95-2、C101-2、C101-3、C111-4、C124-1、C124-2、C167-1、C167-3、C191-2、C204-1、C204-2、C204-3、C205-1、C205-4、C227-2 等） |

> 说明：工具口径 `Open=29` 与"真实待办 16 条"的差额来自**同一 ID 在 Open 段出现多次**的行（如 C124-1/C124-2 同时出现在 batch-124 小节与 Deferred 小节），工具按 ID 去重后计入的行数与该小节残留有关；建议后续批次做一次"同 ID 去重 + C99-1 拆号"（本轮未动，避免与废弃判定冲突）。

## 3. 硬门禁与核对

| 门禁 | 命令 | 结果 |
|---|---|---|
| 条件追踪器审计 | `pwsh scripts/git/audit-cconditions.ps1` | `hard errors: 0`、`warnings: 1`（既有）、`missing evidence: 0` |
| 提交卫生 | `pwsh scripts/git/scan-common-bugs.ps1` | HARD 0（332 WARN 全在改动文件之外） |
| 待办清单核对 | 独立脚本解析 `## Open` 段 | 真实待办 = 16 条，与上方明细逐条一致 |

## 4. 遗留与建议顺序（真实待办 16 条）

| 批次候选 | 内容 | 理由 |
|---------|------|------|
| batch-255 | C252-1（`release.ps1` 失败路径 reconcile）+ C252-2（发布容量回收 SOP） | 今天实际踩到，改动小、直接影响发布安全 |
| batch-256 | C243-1（Runner 容器隔离/只读 rootfs/egress）+ C246-1（dev-only npm audit 7 high） | 生产风险面最大 |
| batch-257 | C215-1（历史文档归档）+ C110-4（P0 口径复核）+ C161-2/3（调度环境绑定 / surface 回填） | 低成本收口 |
| batch-258 | C122-2/4、C123-2/3/4、C124-3（用例长尾 / 知识中心 / 运营后台入库） | 业务价值高，需生产窗口 |

## 5. 复盘卡

| 计划耗时 | 缺陷(P0/P1/P2/P3) | 返工次数 | 根因分类 | 下次避免 |
|----------|-------------------|----------|----------|----------|
| 计划 1h / 实际约 1h | 0/0/0/0 | 1（首版迁移未处理同 ID 重复行，导致 Open 只降 8 条；改为按 ID 全量归类后重跑） | 流程（追踪器数据质量） | 追踪器改动后必须用"独立解析脚本"复核真实待办清单，而不是只看 `stats` 差值 |
