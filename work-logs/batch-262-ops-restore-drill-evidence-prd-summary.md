# Batch 262 — PRD-lite Summary

> **Product (🟦)** | Date: 2026-09-19 | Status: Review

```text
mode: light
豁免理由: 纯文档 / 纯证据批次——只新增 ops 手册与验收报告结论，不改任何应用代码、接口、配置或数据模型。
非目标: 不实现磁盘告警（C262-1）、不增加备份定时任务（C262-2）、不在生产执行迁移（C262-3）。
```

## 1. 问题陈述

用户授权生产访问，用于补完落地方案验收清单的第 ⑧（生产盘水位可控）与第 ⑨（备份可恢复）条。
在生产主机上实测后暴露三件事，必须落盘而不是只留在聊天里：

1. **`docs/ops/restore-drill.md` 不存在**——第 ⑨ 条要求"按该文档复演一次"，而该文档是 B0-3 的交付物，至今待办。
   当时的演练只是临时脚本，不满足"照文档复演"的语义。
2. **85% 磁盘告警不存在**——第 ⑧ 条的第二半（"85% 阈值能触发告警"）不成立，与方案 B0-2「待办」一致，但此前只有文档状态、没有生产侧证据。
3. **生产库落后主干**——`execution_jobs`(B1)、`impact_edge`(B3) 在生产库中不存在，`alembic_version` 停在 `20260922_ai_agent_token`。

## 2. 成功指标

| 指标 | 基线 | 目标 | 测量窗口 |
|------|------|------|---------|
| 恢复演练可复演 | 无文档 | 手册含步骤/证据/判定/失败处置，且整段可复制执行 | 本批 |
| 第 ⑧/⑨ 条结论 | "未能由我验证" | 用生产实测替换为明确结论 | 本批 |
| 新缺口登记 | 无 | 三个运维缺口各成 C 条件并带解除条件 | 本批 |

## 3. 用户故事 + 验收标准

- As a 值班工程师, I want 一份可以直接照着做的恢复演练手册, so that 我不是每次凭记忆临时写脚本。
  验收：Given 生产 SSH 可达 / When 复制 `docs/ops/restore-drill.md` §2 的命令块 / Then 完成建库→恢复→抽查→清理，并得到与 §3 同形制的证据。

- As a 版本负责人, I want 验收清单第 ⑧/⑨ 条有生产实测而不是"待确认", so that 放行判断不建立在推测上。
  验收：Given 报告 / When 读第 ⑧/⑨ 条 / Then 能看到命令、实测值与结论（含"一半达成"这种不圆滑但真实的结果）。

## 4. 技术考量

- 演练**只在临时库上进行**，`cameltv_production` 全程只读；恢复用 stdin 灌入以避免把 dump 拷进容器。
- 生产主机凭据/密钥不进仓库；手册只写连接方式与命令，不写任何密码。
- 新增 C 条件不改变 B1–B4 的既有结论，只把"未知"变成"已知且有解除路径"。
