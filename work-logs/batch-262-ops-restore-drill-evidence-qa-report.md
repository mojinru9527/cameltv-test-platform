# Batch 262 — QA 报告

> **QA (🔍)** | Date: 2026-09-19 | Verdict: **PASS**
> 档位：轻量批次（纯文档/纯证据）。门禁按"变更域=文档"执行。

## 测试总览

| 条件数 | 通过 | 失败 | 阻塞 |
|--------|------|------|------|
| 3（手册可用 / ⑧⑨ 结论 / 缺口登记） | 3 | 0 | 0 |

## 可执行门禁

| 命令 | 退出码 | 摘要 |
|------|:------:|------|
| CI 范围分类期望 | — | 本批仅改 `docs/**`、`work-logs/**`、`C-CONDITIONS.md` → 属"文档/证据"域，前后端重测试按规则跳过，三个 required contexts 仍返回明确结果 |
| `pwsh scripts/git/scan-common-bugs.ps1` | 2 | **HARD 0**；WARN 344，与主干基线逐项一致（同一命令在 `wt-main` 亦为 HARD 0 / WARN 344）。退出码 2 = 存在 WARN 且未启用 `-FailOnWarning`，按门禁规则**非阻断** |
| 文档元数据检查（frontmatter: title/owner/created/status/expires/related） | ✅ | `docs/ops/restore-drill.md` 按 `docs/` 规范携带元数据 |
| **演练本身（生产实测，本批的核心证据）** | 0 | 见下 |
| 验收报告引用证据逐份复跑（本批追加） | — | 8 份后端单测 + 前端全量（168 文件/737 例）+ Playwright a11y（28 passed）全部重跑核对；发现并更正 `tests/test_url_guard.py` 例数 19→17，见验收报告附录 A |

## 逐条件验证

### C1: 恢复演练手册可照做 ✅
**变更**: `docs/ops/restore-drill.md`（新增，含 §1 环境事实 / §2 可复制命令块 / §3 本次证据 / §4 三个缺口 / §5 频率与判定与失败处置）
**实测**：手册 §2 的命令块**就是本次真实执行的命令**（建临时库 → `pg_restore` stdin 灌入 → 抽查 → DROP），结果显示在 §3。

### C2: 第 ⑧/⑨ 条有生产实测 ✅
**变更**: `work-logs/batch-261-...-final-acceptance-report.md` 第 ⑧/⑨ 条
| 检查项 | 结果 | 说明 |
|--------|------|------|
| ⑧ 水位 | 73% | `df -h /` → 40G/28G used/11G avail/**73%**（< 80% 达标） |
| ⑧ 告警 | ❌ 不存在 | `crontab -l` 无任何磁盘水位告警 → 第 ⑧ 条判为**一半达成**（不圆滑但真实） |
| ⑨ 恢复 | ✅ | 67M dump → 临时库 → 197 表 / alembic 可读 / **137,642** 条执行记录可抽查 → DROP；**20 秒** |
| ⑨ 文档 | ✅ | 手册已从"不存在"变为可复演 |

### C3: 缺口登记 ✅
新增 `C262-1`（85% 告警缺失）、`C262-2`（备份节奏不满足每日）、`C262-3`（生产库落后主干，B1/B3/B4 迁移未上生产），各带解除条件。

## 缺陷列表

| # | 严重级 | 描述 | 证据 | 状态 |
|---|:------:|------|------|------|
| D1 | P2 | 第 ⑨ 条要求"按 `docs/ops/restore-drill.md` 复演"，而该文档此前不存在（B0-3 待办）——需求引用了不存在的交付物 | `Test-Path docs/ops/restore-drill.md` = False | ✅ 本批补写该手册 |
| D2 | P3 | 生产主机 `ubuntu`/`lighthouse` 用户登录被拒，实际可用用户为 `root`（手册已写明；此前文档未记录） | ssh 实测 3 次 | ✅ 已写入手册 §1 |

生产侧缺口（G1/G2/G3）已按流程转为 C 条件，不计入本批缺陷（非本批引入、也非本批可修）。

## bug-guard「未关闭已知风险」表核对（三问）

**1) 本批是否新增了清单中的任一项？** 否——本批**零代码改动**，无新增"用户输入→出网/落盘/执行代码"路径。
**2) 本批是否修复/关闭了其中任一项？** 无对应项（S1–S6 已在 B1/B2 处理）；本批未触碰 `app/`。
**3) 新增路径是否过铁律？** 不适用（无新增路径）。生产操作本身只读 + 临时库，凭据未落仓库（手册只写连接方式）。

## CI 分层核对

本 PR 只改 `docs/**`、`work-logs/**`、`C-CONDITIONS.md` → 分类器按"文档"域处理，前后端重测试跳过，但 required 汇总 job 仍会给出明确成功结论；**不把 required 名称存在当作重测试已跑**。

## 发布建议

状态：**READY**　必修复：0　建议修复：0

## 复盘卡

| 计划耗时 | 缺陷(P0/P1/P2/P3) | 返工次数 | 根因分类 | 下次避免 |
|----------|-------------------|----------|----------|----------|
| 3h / ~1.5h | 0/0/1/1 | 0 | 需求（验收条目引用了不存在的交付物） | 验收清单里凡出现"按 `docs/xxx.md` 复演"，先 `Test-Path` 确认该文档存在，不存在就先建再演 |

**技能使用**: 无（本批为运维手册与证据整理，未触发技能；非测试证据）。
