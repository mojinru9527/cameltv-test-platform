# Batch 266 — Leader Verdict

> **Leader (🎯)** | Date: 2026-09-19 | Decision: **有条件通过**
> 待用户一次总确认（推送 + Draft PR + required checks 通过后合入）后转 APPROVED。

## 评审摘要

| 维度 | 评分 | 备注 |
|------|:----:|------|
| 实现质量 | 良 | 断言语义与生成器对齐；未知类型/算子仍 fail-loud；异常收敛不掩盖结论 |
| 风险 | 中 | 属执行链路变更 → 完整批次；但改动限于节点执行器与 QA 驱动，控制面未动 |
| 覆盖 | 良 | 54 例测试（8 新）；真实 Test5 实跑 1 版本留证 |
| 流程合规 | 良 | 完整批次六件工件齐备 |

## 关键决策（已批准）

1. **补齐断言能力而不是放宽判定**：节点补 `status_code+operator` / `jsonpath` / `response_time`，但**未识别仍失败**——避免用"忽略未知断言"制造假通过。
2. **逐用例隔离按"每用例一个 context"落地**：代价是慢（30 条 Web 用例约 8~10 分钟），换来结论不受站点状态影响。
3. **异常收敛**：单步/单用例异常一律记失败；实跑中曾出现"一条用例超时→整节点崩溃"，这是必须堵的洞。
4. **样本质量与链路质量分离**：API 45/50 失败主因是自动生成用例缺真实参数，不当作链路缺陷，登记 `C266-1`；失效链接登记 `C266-2`。
5. **驱动缺定义即失败**：宁可不跑，也不再用 brief 空跑出"30/30"。

## 抽检通过

- ✅ `executor.evaluate_assertions` — 新增类型/算子有测试；未知算子返回"未识别的断言算子"且失败。
- ✅ `run_web_cases` — 每用例新建上下文（测试断言工厂调用次数=用例数）；单用例异常被捕获。
- ✅ `drill_three_versions._load_executable_cases` — api/web 构造正确；`unexecutable` 直接 `SystemExit`。
- ✅ 真实实跑 — `jobs=[21,22] evidence_complete=True`；API 5/50、Web 28/30（失败归因已逐条列出）。

## 判决

**有条件通过**，合入前置：

1. 用户一次总确认（推送 + Draft PR + required checks 通过后合入 main）。
2. `pwsh scripts/git/audit-ai-pr.ps1 -ExpectedWorkflow agent-team -ExpectedExecutor codex -RequireSuccessfulChecks` 通过。

## 下一批次 Leader 条件

- **C266-1（P1）**：试点 API 用例需**真实参数**。解除条件=用环境/账号槽位提供的真实参数重生成或精修 ≥50 条接口用例，复跑后给出真实通过率（并说明仍未通过项的归属）。
- **C266-2（P2）**：用例内写死的详情页链接会失效。解除条件=引入"运行时取链接"或改用稳定入口页（列表页断言），并复跑 30 条 Web 用例达稳定通过。
- **C265-1（P1，沿用）**：复用命中率需走版本任务流程取真实数字。

## 流程回写（Batch 75 起强制）

| 发现 | 处理 | 落点 |
|------|------|------|
| 生成器与执行器的断言 schema 长期不一致（`status_code+operator` / `jsonpath` / `response_time` vs `status` / `json_path`） | 本批在节点侧补齐并加测试；建议后续统一"断言 schema"事实源 | 本批代码 + QA D2 |
| "一条用例异常拖垮整节点" | 本批收敛；建议节点侧再加"job 级兜底" | QA D4 |
| 本机资源不足导致实跑反复失败（OOM、僵尸任务） | 写入复盘卡；实跑前 `wsl --shutdown`、清僵尸任务 | 复盘卡 |

## 复盘卡

| 计划耗时 | 缺陷(P0/P1/P2/P3) | 返工次数 | 根因分类 | 下次避免 |
|----------|-------------------|----------|----------|----------|
| 5h / ~4h | 0/4/2/0 | 3 | 需求（断言 schema 未对齐）+ 环境（本机资源） | 同 QA 复盘卡 |

**技能使用**: `cameltv-bug-guard`（三问核对：新路径=只读库读取，无外发）；`cameltv-api-test`（断言口径参考）。
