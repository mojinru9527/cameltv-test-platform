# Batch 269 — Leader Verdict

> **Leader (🎯)** | Date: 2026-09-20 | Decision: **有条件通过**
> 待用户一次总确认后转 APPROVED。档位：**轻量批次**（纯证据/纯文档）。

## 评审摘要

| 维度 | 评分 | 备注 |
|------|:----:|------|
| 证据质量 | 优 | ⑦ 是**真实执行链**的可复现报告（6 个 job + 逐文件 sha256 校验）；⑧ 是生产**只读**复核 |
| 风险 | 低 | 零代码改动；对生产的操作只读（未改配置、未重发邮件） |
| 覆盖 | 良 | §5 九条逐条给出结论 + 证据；12 条 C 条件关闭 + 4 条新增（`C265-1` 复核后保持 Open） |
| 诚实度 | 优 | 明确写出"累计口径/人日主观/本机试点控制面/未撑到 85%"四处限制，并在附录 C 列缺口 |

## 关键决策（已批准）

1. **⑦ 由"✅ 达成"下调为"⚠️ 判定内核达成、口径受限"**：判定内核 `meets_all=true` 成立（执行时长/证据完整/连续 3 版是本次自产），但两项经取证不符 §5 字面：① 驱动只登记执行任务、**不建版本任务** → 16.1/16.2/16.3 **没有版本记录**；② **复用率 0.625 不是这 3 个版本产生的**（不调 `POST /version-tasks`，演练窗口内新增复用事件 0 条、新增版本任务 0 个，数字来自 00:26–00:32 的平台读数）。因此 `C269-1` 由 P2 **上调为 P1**：驱动必须逐版本走版本任务流程并给"本版自产"数字。人日仍是执行者输入。
2. **不把本机试点控制面说成生产/测试环境**：控制面是 `127.0.0.1:8124`（batch-268 代码 + 试点 SQLite），节点是本机 `cameltv-node`；**被测系统是真实 Test5**。这条在验收报告 §3-⑦ 与附录 C 都写明。
3. **⑧ 判"达成"以三段证据合成**：阈值分支实测 + `MAIL_OK` + 用户确认收到；**不宣称**盘到过 85%。
4. **C 条件收口从严**：12 条关闭**每条都要有 PR/commit/证据**；凡"口径被替换"的（`C266-1`）必须在关闭行里写明"替换而非精修"，不许含糊成"已精修"。
5. **本轮暴露的 4 个能力缺口不塞进本批修**：登记 `C269-1`~`C269-4`，避免"顺手改驱动"把验收批次变成开发批次（会触发完整批次档位）。
6. **B4 后是否删除老队列**：Batch 263 已裁定保留 `ui_test_service` 并登记 `C263-1`，本批不推翻该裁定。

## 抽检通过

- ✅ `evidence/batch-269/drill-three-versions-platform-reuse.json` — 与驱动 stdout、平台 job 记录（46–51）三者一致；`evidence/verify` 对 job 46/51 抽检 `verified`。
- ✅ `evidence/batch-269/prod-disk-alert-and-watermark-20260920.md` — 脚本逻辑逐字摘录与主机上文件一致；`smtp.env` 仅核对键名与权限，**密码未落仓库**。
- ✅ `C-CONDITIONS.md` — 12 条「已关闭」均带证据字段（`C265-1` 复核后保持 Open）；`audit-cconditions.ps1` hard errors 0 / warnings 0。
- ✅ `batch-269-landing-acceptance-report.md` — §5 九条 + 附录 A/B/C（复跑命令、风险表核对、未验证项）。

## 判决

**有条件通过**，合入前置：

1. 用户一次总确认（推送 `feature/batch-269-landing-closeout` + Draft PR + required checks 通过后合入 main）；
2. `pwsh scripts/git/audit-ai-pr.ps1 -ExpectedWorkflow agent-team -ExpectedExecutor codex -RequireSuccessfulChecks` 通过；
3. 用户对 §5 九条结论（尤其 ⑦ 的**口径限制**）给出验收意见——若有异议，先改报告再合。

## 下一批次 Leader 条件

| ID | 优先级 | 一句话 | 解除条件 |
|----|:------:|--------|---------|
| `C269-1` | **P1** | 3 个版本**没有产生**复用数据（不建版本任务 → 0 条事件）；复用率取的是平台既有读数 | 驱动逐版本走版本任务流程（建任务 → 平台自动写 `suggested` → 操作者记采纳/否掉）并按版本区间取增量；报告分列"本版自产"与"平台累计" |
| `C269-2` | P2 | 驱动加**瞬断重试 + 每版增量落盘**（本批 4 次白跑） | 有界重试仅针对瞬时 transport 错误；每版写一次报告文件；补单测 |
| `C269-3` | **P1** | 节点遇平台 4xx/5xx 即 `SystemExit(2)` 退进程（轮询循环只捕获 `TransportDown`）——根因已复现 | 轮询循环按可恢复处理 + 连续失败上限 + 退出必留日志 + 三场景回归 + 重启重连实测 |
| `C269-4` | P3 | Web `case:605` 断言锚点错（3 轮唯一失败项） | 改锚到目标页稳定元素，复跑 3 轮 30/30 |

## 流程回写（Batch 75 起强制）

| 发现 | 处理 | 落点 |
|------|------|------|
| 长跑验收驱动缺容错，崩溃丢全部进度 | 登记 `C269-2`；复盘卡沉淀"长跑驱动先做崩溃可恢复" | `C-CONDITIONS.md` + QA 复盘卡 |
| "指标可测"与"指标口径正确"是两件事 | 本批把累计/增量口径写进验收报告并登记 `C269-1` | 验收报告 §3-⑦ + `C269-1` |
| 节点进程自愈未验证 | 登记 `C269-3`（B1-7 "一条命令可用"不等于"无人值守可用"）；本批进一步用桩复现出"平台 4xx/5xx → SystemExit → 退进程"的机制并上调 P1 | `C-CONDITIONS.md` + `evidence/batch-269/node-exit-root-cause-20260920.md` |

## 复盘卡

| 计划耗时 | 缺陷(P0/P1/P2/P3) | 返工次数 | 根因分类 | 下次避免 |
|----------|-------------------|----------|----------|----------|
| 5h / ~4h | 0/0/3/1 | 4（演练重跑） | 环境（试点实例连接稳定性）+ 设计（驱动无容错/无增量落盘） | 见 QA 复盘卡三条 |

**技能使用**: `cameltv-bug-guard`（三问 + S7/S8 复发核对）。
