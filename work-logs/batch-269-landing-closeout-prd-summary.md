# Batch 269 PRD-lite — B1→B4 收口 + §5 最终验收报告

> **Product/PM** | Date: 2026-09-20 | 档位：**轻量批次**
> `mode: light`
> 豁免理由：**零代码改动**的「证据固化 + C 条件收口 + 最终验收报告」——不新增接口/依赖/Schema，不触碰执行链路、AI 链路与数据模型（判定见 `docs/agent-team/pipeline-modes.md`：纯证据/纯文档类）。

## 1. 触发

用户 2026-09-20 消息：

1. 授权合入 Batch 267 与 268（已完成：267 = #486 `f8e258bd`，268 = #487 `27c11e80`）；
2. 「QQ 邮箱有收到 cameltv 邮件自检」→ §5 第 ⑧ 条的**送达**这一段可以判定了；并要求 Batch 266 收尾；
3. 「允许」。

另外，`§5 ⑦ 体育连续 3 个版本达成 SLO` 一直是 B1→B4 的最后一个未达成项（`C261-1`/`C265-1`/`C268-1`），本轮已在真实执行链上跑通（见 QA 报告）。

## 2. 本批做什么

| # | 动作 | 产出 |
|---|------|------|
| A1 | 固化 §5 第 ⑦ 条的真实 3 版本演练证据（第 5 次尝试成功） | `work-logs/evidence/batch-269/drill-three-versions-platform-reuse.json` + 驱动 stdout |
| A2 | 固化 §5 第 ⑧ 条生产水位与告警只读证据 + 送达确认 | `work-logs/evidence/batch-269/prod-disk-alert-and-watermark-20260920.md` |
| A3 | 批量收口 C 条件（12 条「已关闭」，全部带 PR/commit/证据；`C265-1` 经复核保持 Open） | `C-CONDITIONS.md` |
| A4 | 登记本轮新发现的 4 条缺口 | `C269-1`~`C269-4` |
| A5 | 输出 §5 九条最终验收报告（用户验收用） | `work-logs/batch-269-landing-acceptance-report.md` |

## 3. 非目标（明确不做）

- ❌ **不修** `C269-1`~`C269-4`（属下一批：驱动口径/容错、节点自愈、Web 断言锚点）；
- ❌ **不做真机**（`C258-1`，用户已明确本轮排除）；
- ❌ **不改**任何 `test-platform-v2/**` 代码、迁移、配置；
- ❌ **不把**⑦ 的复用数字包装成"每版本独立增量"（当前是平台累计口径，如实写明并登记 `C269-1`）；
- ❌ **不重发**告警邮件（本轮对生产的操作全部只读）。

## 4. 验收判据

| # | 判据 |
|---|------|
| A1 | 3 版本演练报告存在且 `meets_all=true`、`consecutive_passing=3`，三个版本 `evidence_complete=true` |
| A2 | 每个版本的执行 job 都能在平台上取到，且 `evidence/verify` = `verified` |
| A3 | 生产只读复核给出 `df < 80%`、cron 每 15 分钟的水位检查、`ALERT` 分支实测触发、`MAIL_OK` 发送成功 |
| A4 | ⑧ 送达有用户口径确认；不把"降阈值强制触发"说成"盘真的到过 85%" |
| A5 | 12 条 C 条件关闭均带证据；4 条新增均有解除条件（`C265-1` 保持 Open 并转 `C269-1`） |
| A6 | 报告如实列出**没做到/没验证**的部分 |

## 5. bug-guard 预检

本批零代码改动 → 无新增"用户输入 → 出网/落盘/执行代码"路径；仍需核对「未关闭已知风险」表（见 QA 报告三问）。
