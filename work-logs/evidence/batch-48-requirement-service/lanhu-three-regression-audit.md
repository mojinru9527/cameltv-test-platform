# Batch 48 蓝湖三条阻塞专项回归

## 边界与版本

- 执行日期：2026-07-27
- 根仓实现提交：`b9a2066d273a097cccbd2456bae062ad45aa297c`
- `lanhu-mcp` 本地提交：`74bfa7b463ef505008ea25466bc950ad9ed67324`
- 真实输入：复用本地忽略配置中的既有蓝湖 URL 与 Cookie。
- 脱敏：不记录 URL、Cookie、页面名称、页面 ID 原文、响应正文或 OCR 文本。
- 范围：仅 `B47-MOD-004`、`B47-MOD-006`、`B47-MOD-010`。

## 公共有界下载结果

| 检查 | 结果 |
| --- | --- |
| 认证与页面枚举 | 成功，枚举 106 页 |
| 实际下载页面 | 1 页，与 URL 中目标 `pageId` 一致 |
| 下载耗时 | 34.154 秒 |
| 本地文件 | 295 个，共 2,547,710 字节 |
| HTML | 1 个 |
| 临时分片残留 | 0 |
| 安全边界 | 最多 500 资源、100 MiB、总时限 120 秒 |

下载器同时具备目标版本/页面选择、资源数量上限、总字节上限、整体超时和取消清理；受限或部分失败结果不会继续生成正式证据。

## B47-MOD-004 — 重复/并发提取幂等

- 连续重复调用复用相同自然身份，返回同一组模块 ID，节点计数不增加。
- 自动提取仅更新自动字段，不删除手工节点，也不覆盖人工说明或交互数据。
- 真实 PostgreSQL 使用隔离克隆 `codex-batch48-oldpg-final-20260727`，临时数据库 `batch48_lanhu_concurrency`，schema head 为 `20260727_batch48_pg_parity`。
- 4 路并发 HTTP 请求全部返回 200 和相同模块 ID。
- 最终仅 1 个 module + 1 个 page，无重复或半棵树；`module:extract` 审计 4 条，与接受请求数一致。
- 临时数据库已删除，克隆容器已停止，原旧库卷未修改。

结论：PASS。

## B47-MOD-006 — 附件失败转人工处理

- 真实 URL 正常资源继续下载；不再要求额外损坏附件 URL。
- 受控故障覆盖“1 个正常附件 + 1 个不可读附件”：
  - 正常项照常生成摘要和业务规则。
  - 不可读项计入 `failed`，不计入 `processed`，返回“附件内容不可读取，请人工处理”。
  - 失败项不覆盖原说明，不生成知识实体或其他业务副作用。
  - 重试后业务规则实体仍为 1 条，无重复。
- 资源下载层的单项失败保留成功文件，并返回 `manual_action_required` 和失败资源清单。

结论：PASS。

## B47-MOD-010 — 截图与 OCR

| 检查 | 结果 |
| --- | --- |
| 目标页本地 HTML | 存在且可渲染 |
| 截图 | 7 段，共 562,714 字节 |
| 截断 | 否 |
| 截图哈希 | 7 个均唯一 |
| OCR | 中文本地 OCR，637 个文本块 |
| OCR 置信度 | 最低 0.6007，平均 0.8546 |
| `merged_text` | 非空 |
| 可见浏览器复核 | 页面可渲染、截图可见 |
| 浏览器控制台错误 | 0 |
| 浏览器失败请求 | 0 |

中文 OCR 模型来自官方 `tessdata_fast`，本轮使用文件 SHA-256：
`A5FCB6F0DB1E1D6D8522F39DB4E848F05984669172E584E8D76B6B3141E1F730`。
模型和原始截图仅用于本地回归，未提交到 Git。

结论：PASS。

## 自动化结果

| 范围 | 结果 |
| --- | --- |
| `lanhu-mcp` 全量 | 17 passed |
| 后端蓝湖/模块专项 | 106 passed |
| 真实 PostgreSQL MOD-004 并发 | 1 passed |
| Ruff F821/F823 | 通过 |
| `git diff --check` | 通过 |

## 交付说明

三条行为回归均已通过，本地 48 条验收用例达到 48/48。当前账号对上游
`dsphper/lanhu-mcp` 没有写权限，`git push --dry-run` 返回 HTTP 403。
因此根仓尚不能安全推送引用该子模块提交；需要先将
`74bfa7b463ef505008ea25466bc950ad9ed67324` 发布到可访问的子模块远端，
或由上游合并后更新 gitlink。
