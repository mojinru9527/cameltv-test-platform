---
title: "体育全链路验收 2026-09-04 — 待提供清单与本地文档缺口"
owner: "qa-team"
created: "2026-09-04"
last_reviewed: "2026-09-04"
status: "active"
expires: "2026-12-03"
tags: ["sports", "blackbox", "acceptance", "swiftbugs", "gaps"]
---

# 体育全链路验收 2026-09-04 — 待提供清单与本地文档缺口

> 本轮所有结论均来自 2026-09-04 在 `https://swiftbugs.cn`（全新项目 `SPORTS-E2E-260904`）与体育站点
> 通过真实浏览器前端操作产生的证据；截图见同目录 `01–13*.png`。
> 不引用昨日报告的执行结果，不保存密码、Cookie、Token 或 API Key 明文。

## 本轮已完成的资产（全部前端点击创建）

| 资产 | 内容 | 证据 |
|---|---|---|
| 项目 | `CamelTv 体育全链路验收 2026-09-04`（`SPORTS-E2E-260904`，角色：负责人） | `03-project-created.png`、`04-empty-project-workbench.png` |
| 环境 1 | `体育生产基线（1.0.0-15.0.0）`：prod / 公网 / 平台直连 / `https://www.camel1.tv` + 6 个变量 | `05-env-prod-filled.png`、`08-prod-env-variables.png` |
| 环境 2 | `体育 Test5（16.0.0）`：test / **纯内网 / 内网执行器 / runner_key=test5-internal-01** / `https://camelive-g3-test5.elelive.cn` + 10 个变量 | `07-env-test5-filled.png`、`09`、`10-test5-env-internal-runner.png` |
| 需求 | `体育生产基线需求-1.0.0-15.0.0-本次黑盒采集.md`（Markdown，**已解析**） | `13-requirement-uploaded-parsed.png` |
| AI 配置 | **未配置（0 个提供方）** | `11-ai-config-not-configured.png` |
| 用例/接口/UI/Mission/发布包/报告/缺陷 | 均为 0（干净起点） | `04-empty-project-workbench.png` |

---

## A. 阻断项：必须由你提供（按优先级）

### A1【P0，硬阻断】有余额的 DeepSeek API Key（或任一可用 AI 提供方 Key）

**为什么是硬阻断**：AI 提供方配置是**按项目隔离**的，新项目 `SPORTS-E2E-260904` 的提供方数为 0，
因此需求页的「功能拆分」「AI 生成」两个按钮实测为 `disabled`，tooltip 原文：

```
当前项目未配置 AI 提供方
```

唯一可复用的旧项目提供方「DeepSeek 官方」今天点击「测试连通性」返回：

```
AI 提供方余额或配额不足——请到提供方控制台充值后重试（提供方：DeepSeek 官方）
原始错误：Client error '402 Payment Required' for url 'https://api.deepseek.com/chat/completions'
```

（证据 `12-ai-402-quota-exhausted.png`；昨日同一测试约 440 ms 成功，说明是配额耗尽而非配置错误。）
编辑弹窗**不回显 Key**（这是正确的安全行为），所以旧 Key 无法复制迁移。
本地 `test-platform-v2/backend/.env` 里的 `AI_API_KEY`（`****c535`）实测直连 DeepSeek 返回
`401 Authentication Fails … api key: ****c535 is invalid`，同样不可用。

**没有 Key 就跑不到的环节**：需求功能拆分、AI 用例生成、Mission 范围分析/歧义澄清/契约生成/场景生成、
DSH 场景任务、版本验收 AI 方案 —— 即 V1 与 V2 两条链路的**全部 AI 阶段**。

**需要你做的**：提供一个有余额的 Key（DeepSeek 官方，或平台「AI 配置」页一键模板里的其他提供方），
我会通过前端在 `SPORTS-E2E-260904` 内新建提供方并跑连通测试。Key 只在安全槽位保存，本清单不落盘明文。

### A2【P0】能真正登录的业务测试账号密码

昨日凭据在 Test5 登录表单提交后页面明确返回 `Wrong password`；生产站 `www.camel1.tv` 的手机号账号
提交后输入被清空、弹窗不关闭、无任何成功/失败提示。
被阻断的用例面：收藏、聊天室发送、银钻、商城、装扮、免费/付费直播分支（约 43 条需登录态用例）。

**需要你做的**：提供一个在 `www.camel1.tv`（V1）和 `camel-bball-test5.elelive.cn`（V2）都能登录成功、
且账号内有可验证业务数据（收藏记录/聊天权限/银钻余额/商城商品）的账号与密码。

### A3【P1】可导入的 OpenAPI JSON/YAML 地址（`basketball-service` / `account-service`）

实测：篮球 Swagger UI 能打开但错误展示 **Petstore** 示例；v3 文档接口返回业务异常；
v2 与 `/swagger-resources` 返回 404。平台后端因此没有任何可导入的契约文档。
连带影响：「新业务接入」表单其他字段（业务名、`basketball-service`、`16.0.0`、需求正文、篮球站地址）
都已由前端填好，**提交按钮仅因 OpenAPI 地址为空而保持禁用**；
接口资产也只能停在 `sports-public-web-blackbox GET /` 根路径，无法绑定真实服务。

**需要你做的**：提供平台服务器（腾讯云广州出口）可访问的真实 OpenAPI JSON/YAML URL，
或修复 `basketball-service`/`account-service` 的 Swagger/Knife4j 配置。

---

## B. 执行层：需要你授权或由平台/运维处理

### B1【P1，需你一句授权】在本机启动内网执行器（runner）

已确认 `camelive-g3-test5.elelive.cn` 解析为 **192.168.50.170**（RFC1918，无公网 A 记录，
Google DoH 查询为空）。平台服务器在公网，**永远不可能直连 Test5**，内网 runner 是唯一通路。
本机在内网内，今天实测该站点 200 可达，具备承载 runner 的条件。

环境侧配置已由我在前端完成（`access_type=internal`、`execution_mode=runner`、`runner_key=test5-internal-01`）。
剩余只需按 `test-platform-v2/docs/上线部署与内网执行器配置清单.md` §4 启动脚本：

```bash
export PLATFORM_URL=https://swiftbugs.cn
export API_USERNAME=<平台账号>        # 需 apitest:execute 权限
export API_PASSWORD=<平台密码>
export RUNNER_KEY=test5-internal-01
export PROJECT_ID=<新项目数字 ID>      # 待我确认
export RUNNER_BASE_URL=http://camel-api-gateway05.svc.elelive.cn
python backend/scripts/executor/api_runner.py --once
```

成功标志：日志 `runner[test5-internal-01] 已连 https://swiftbugs.cn`，随后 `[done] 任务#… -> HTTP 200 … all_pass=…`。

**需要你做的**：确认「允许我在本机后台启动该 runner 进程」。这属于你已选的「配置侧修复」范围，
但它是一个长驻外部进程，所以我不会未经明确授权就启动。

### B2【P1，平台/运维】Runtime Worker 上线节点

Durable Runtime 的 `aitde-worker` 离线、能力显示「无」、操作列为空；平台自动检查提示
「Runtime Worker 需要处理」。Workflow/审批列表为空，Policy 与 Secret 页签只有
「见 API /policy-profiles」「见 API /secret-refs」占位文字，**没有任何前端管理入口**。
黑盒用户无法自助上线 Worker，也无法创建数据源（新建表单依赖 `secret_ref`）。

### B3【P1，平台/运维】生产站 WAF 放行平台服务器出口 IP

昨日平台侧执行生产接口用例 16 条仅 4 条通过，其余被 403（WAF）拦截；本机浏览器直连 `www.camel1.tv` 为 200。
即：**纯前端黑盒验证不受影响**，但平台服务器发起的接口执行需要 WAF 白名单，或在平台主机上挂 vpn07。

---

## C. 本地文档 `docs/测试平台全功能验收文档-环境链接与账号汇总.md` 需要修正的地方

这些是「文档自身缺口」，不需要外部资源，但会导致下一个使用者重复踩坑：

| # | 问题 | 建议 |
|---|---|---|
| C1 | frontmatter `status: active`、`expires: 2027-02-05`，正文却写「已迁移/退役（2026-08-22 → swiftbugs.cn）…内容仅作历史存档」 | 状态语义冲突。改为 `status: active` 且删除「仅作历史存档」，或整体降级为 archived 并新建现役文档 |
| C2 | §5.6–5.9、§2.5 仍保留 Vercel / Railway / Supabase / Cloudflare 等已于 2026-08-23 退役的环境 | 与现役腾讯云入口混排，无法直接作为生产可用性验收依据；退役段落应移入历史附录 |
| C3 | §1.4 写用户端版本 **14.1.0**，而本次 V1 基线定义为 **1.0.0–15.0.0** | 需你确认生产当前真实用户端版本，并统一文档口径 |
| C4 | §2.3「GitHub Actions 工作流（共 8 个）」与 §5.5「工作流数量 7 个」互相矛盾 | 以仓库 `.github/workflows/` 实际数量为准，二者取一 |
| C5 | 缺少 16.0.0 篮球 Test5 站点 `https://camel-bball-test5.elelive.cn/basketball`（今天实测 200，根路径 307） | 你说「域名还在申请」，但该 Test5 域名已可用 —— 请澄清：是**生产**域名待申请、Test5 已就绪？确认后写入文档 |
| C6 | 缺少稳定服务标识清单 | 已由页面真实请求动态确认：`basketball-service`、`account-service`、`camel-service`。文档应明确「service key ≠ 被测 URL」 |
| C7 | 只描述 `runner_key` 底层字段，没有用户自助获取/生成/轮换流程，也没有 Worker 上线指引 | 补一节「内网执行器上线 SOP」，含 §B1 的启动配方与成功标志 |
| C8 | 缺少「平台服务器 → 生产站」访问前置条件（WAF 白名单 / vpn07） | 补明：平台侧接口执行生产域名前必须先放行，否则 403 |
| C9 | 缺少角色化业务测试账号与可定位测试数据清单 | 需覆盖：直播中 / 未开赛 / 已结束 / 回放 / 免费 / 付费 / 广告 / 新闻 / 联赛 / 球队 / 球员 / 16.0.0 篮球样例 |
| C10 | §1.1 同时写「vpn07 tun 模式」与「当前临时状态 vpn_required=false / proxy_strategy=direct」，未说明哪个生效 | 明确当前生效策略与切换条件 |

---

## D. 今天新发现的平台前端缺陷（已通过前端「缺陷管理 → 新建缺陷」全部录入 `SPORTS-E2E-260904`）

录入后缺陷列表共 **15 条**（P0-致命 1、P1-严重 3、P2-一般 9、P3-建议 2，全部待处理，已用筛选器逐项复核）；
证据 `14-defect-page-empty.png`、`15-defects-filed-8.png`、`16-defects-filed-9.png`、`17-mission-M-13-0001-created.png`、
`18-mission-source-doc18.png`、`19-source-fragments-3-chunks.png`、`20-scope-3items-no-list.png`、`21-defects-15-including-P0.png`。

| 平台编号 | 级别 | 现象 | 影响 |
|---|---|---|---|
| DEF-20260904-001 | P2 | 侧边栏「版本验收任务」「版本发布包」「智能测试任务」「报告中心」「缺陷管理」5 项是无 `href`/`tabindex`/`role` 的 `<a>`，无障碍树退化为 `generic`，键盘不可聚焦，无法深链/后退 | 5 个模块入口，含版本验收与缺陷闭环两条核心链路 |
| DEF-20260904-002 | **P1** | AI 两条互不相通的凭据链路当前都坏：全局 `AI_API_KEY` 报 `401 Authorization Required`，项目级提供方池报 `402 Payment Required`；编辑弹窗不回显 Key 导致无法迁移；平台无统一 AI 健康面板 | 阻断 AI 全链路全部阶段（功能拆分、AI 用例生成、Mission 范围/契约/场景、DSH、版本验收 AI 方案） |
| DEF-20260904-003 | P2 | 环境表单选「访问类型=纯内网」后执行模式**不自动切到 runner**，仅变为可选并停留在「平台直连」，无强制校验或警告 | 可保存「内网 + 平台直连」矛盾环境，直到执行期才失败 |
| DEF-20260904-004 | P2 | 直连 URL 契约不一致：`/projects`、`/requirements` 均 404（真实路由 `/my-projects`、`/requirement`），与 `/runtime`、`/missions/{mission_key}` 404 同类 | 文档与缺陷单里的链接大量失效，无法深链定位资源 |
| DEF-20260904-005 | P2 | 需求上传区装饰性 `lucide-inbox` SVG 拦截指针事件，点击「上传需求文档」被 intercept 并重试 | AI 全链路第一步点击无响应，用户误判为不支持上传 |
| DEF-20260904-006 | P2 | 工作台生成类任务静默失败：`generate 任务失败 'coroutine' object has no attribute 'get'` | 后端缺 `await` 导致必然失败，且把 Python 内部细节直接暴露给终端用户 |
| DEF-20260904-007 | P3 | 环境详情面板不展示 Base URL / 访问类型 / 执行模式 / runner_key | 必须重开「编辑环境」才能核对刚配置的内容 |
| DEF-20260904-008 | P2 | 缺陷编号仅按日期生成不含项目维度：`DEF-20260904-001` 在 `SPORTS-E2E-260903` 与 `SPORTS-E2E-260904` 指向两个不同缺陷 | 跨项目沟通歧义，无法凭编号唯一定位缺陷 |
| DEF-20260904-009 | P2 | 顶部统计卡把 P3 并入 P2 计数且不展示 P3 卡片：卡片 P2=7，筛选器实测 P2=6、P3=1、P1=1、P0=0、全部=8 | 概览与明细对不上，P3 积压在管理视角被隐藏 |
| DEF-20260904-010 | **P0** | Mission 范围页对 `GET /api/v2/missions/{id}/scope` **无终止轮询**：实测约 45 次/秒，10 秒内计数 3204→4119→4574，2 分钟累计 4574 次；进度恒为 0% 使轮询条件永远为真，只有离开页面才停止 | 单个用户打开一个范围页即对生产后端制造约 45 QPS 无效负载，多标签线性放大等同自我 DoS；浏览器请求队列与内存持续增长直至卡死 |
| DEF-20260904-011 | **P1** | 范围接口已返回 3 条完整 items（id 209/210/211，`review_status=PROPOSED`），前端**一条都不渲染**：页面只有 3 个计数器和 1 个按钮，无列表、无卡片、无空态 | Mission 主链在第 2 阶段硬性中断，永远进不到契约；**与 AI 是否可用无关**（这 3 条是 `created_by_type=DETERMINISTIC`，本次全程未配置 AI） |
| DEF-20260904-012 | **P1** | Source 解析按**定长字符切块**而非需求边界：约 30 个 `BASE-*` 需求点被压成 3 块；REQ-0001 结尾正好是标题 `#### BASE-MATCH-002`，其正文被切到 REQ-0002 开头成为孤立行；Markdown 换行被压平、frontmatter 被内联 | 覆盖率无法按需求点闭合；片段名统一为「需求片段 N」丢失原始需求编号，需求→用例双向追溯断裂。这是昨日「16.0.0 AI 用例完全漏掉篮球」的根因之一 |
| DEF-20260904-013 | P2 | 确定性降级产物被标注 `ai_confidence=0.95`：无任何模型参与却给出 95% AI 置信度；且 `created_by_type` 前端完全不展示 | 置信度字段失效；用户无法区分 AI 结果与规则降级产物，会在错误前提下继续往下走。与昨日「零资产差异对比返回置信度 100%」同类 |
| DEF-20260904-014 | P2 | Mission「添加 Source」选「需求文档」后要求填裸 **需求文档 ID**，无下拉/搜索/说明；需求文档列表表头（标题/来源/状态/导入/操作人/时间/操作）**无 ID 列**，预览也不显示 ID | 纯前端用户无法完成主链第 1 阶段。本次唯一可行路径是到审计日志反查 `requirement:upload #18` 才得到 ID=18。与昨日「愈合评审/Flaky/Fixture 只接受裸内部 ID」同类 |
| DEF-20260904-015 | P3 | 审计日志「操作人」口径不一致：9 条 `defect:create` 显示登录名 `sportsadmin`，`requirement:upload` 显示显示名「体育平台管理员」 | 按操作人筛选/统计时同一人被拆成两个身份，以显示名记录的事件在按登录名筛选时被漏掉 |

DEF-20260904-008 与 -009 是在录入前 7 条的过程中新发现的（前者因新旧项目同号，后者因核对统计卡与筛选器计数不一致）。
录入 15 条后再次复核，统计卡显示 P2-一般=11，而筛选器实测 P2=9、P3=2（9+2=11），**-009 用新数据再次复现**。
DEF-20260904-010 至 -015 是在「不依赖 AI、纯前端走 Mission 主链」过程中发现的，其中 -010 为本次最严重问题。
DEF-20260904-009 与昨日 `DEF-20260903-017`（报告中心汇总指标与实际不一致）属同一类聚合口径错误，建议一并排查后端统计接口。

---

## D2. Mission 主链「无 AI」实测可达边界（回答「平台现在能做到哪一步」）

在项目 `SPORTS-E2E-260904`（数字 ID = **13**，取自请求头 `x-project-id`）中，
在 **AI 提供方为 0** 的条件下，纯前端逐段走 Mission 主链，实测结果如下：

| 阶段 | 前端操作 | 结果 | 判定 |
|---|---|---|---|
| 0. 创建 Mission | 「新建 Mission」三步向导（基本信息 → 确认 → 完成） | 成功创建 **M-13-0001**（id=37），状态 草稿，验收 NOT_EVALUATED，跳转 `/missions/37/overview` | ✅ 通过 |
| 1. 资料 Sources | 添加 Source → 来源类型「需求文档」→ 填 ID 18 → 添加 → 解析 | 成功关联 `需求文档 #18`，解析状态「已解析」，片段数 3 | ⚠️ 通过但有两处缺陷：ID 无法从前端发现（DEF-014）；切块方式错误（DEF-012） |
| 2. 范围 Scope | 点击「分析范围」 | 计数从「范围 0 项」变为「范围 **3 项**」，接口 200 返回 3 条完整 items；但**页面不渲染任何条目**，已评审 0、进度 0%，且开始约 45 次/秒的无终止轮询 | ❌ **硬性中断**（DEF-011 渲染缺失 + DEF-010 轮询失控） |
| 3. 契约 Contract | — | 未进入 | ⛔ 被阶段 2 阻断 |
| 4. 场景 Scenario | — | 未进入 | ⛔ 被阶段 2 阻断 |
| 5. 执行 / 回放 | — | 未进入 | ⛔ 被阶段 2 阻断 |
| 6. 验收 Acceptance | — | 未进入 | ⛔ 被阶段 2 阻断 |

**关键结论（对昨日判断的修正）**：

1. 阶段 2 的失败**不是由缺少 AI Key 造成的**。这 3 条 items 的 `created_by_type=DETERMINISTIC`，
   是在完全没有 AI 提供方的情况下由确定性规则生成并成功入库的（id 209/210/211，`review_status=PROPOSED`）。
   数据在后端存在，前端就是不渲染。
   → 因此**即使你现在给我有余额的 AI Key，范围评审依然无法进行**，主链仍会停在第 2 阶段。
   AI Key 解锁的是「分析质量」，DEF-20260904-011 阻断的是「链路能否往下走」，两者是独立的两个问题。
2. 昨日记录的「Mission 范围分析后结果卡片永久骨架屏」，今天精确定位为两个可分别修复的缺陷：
   **渲染缺失**（页面无列表无空态）+ **轮询无终止**（进度恒为 0% 导致轮询条件永远为真）。
   后者是「永久骨架屏」的成因，也是昨日浏览器进程资源崩溃的合理解释。
3. 昨日「16.0.0 AI 用例完全漏掉篮球核心变更」的根因，今天找到了上游证据：
   Source 解析是**定长字符切块**，需求边界被切断、Markdown 结构被压平、原始需求编号丢失。
   在这样的输入上，任何生成模型都不可能稳定覆盖到具体需求点。
4. 因此，**优先级应当调整**：先修 DEF-20260904-010（P0，正在对生产持续施压）与 DEF-20260904-011（P1，主链唯一硬阻断），
   再谈 AI Key 与 runner。否则给了 Key 也跑不通。

---

## E. 不需要你提供、我可以立即继续的部分

- 生产站 `www.camel1.tv` 纯前端黑盒走查（首页/赛事/详情/赛况/统计/搜索/多语言），沉淀为 V1 功能用例资产
- 篮球 Test5 站 `camel-bball-test5.elelive.cn/basketball` 前端走查（页面结构、动态数据、桌面/移动搜索、播放器、语言菜单、a11y）
- 平台各模块前端逐项走查 + 缺陷证据（用例库、接口资产、UI 自动化、Mission、发布包、报告、缺陷、系统管理、Ctrl+K 隐藏模块）
- 在 `SPORTS-E2E-260904` 内创建非 AI 的结构性资产（Mission 壳、发布包、接口资产、定时任务、数据集）
- runner 部署演练与验证（以 B1 授权为前提）

---

## 给你的最小动作（2026-09-05 复测后重排）

> 复测结论见 [复测结论-20260905.md](复测结论-20260905.md)：昨天排在前两位的平台缺陷**都已修复**，
> 主链阻断点已从「阶段 2 前端不渲染」后移到「阶段 2 契约无内容」。所以现在球在你这边。

### 已完成（无需再动作）

- ~~**修 DEF-20260904-010（P0）**~~ ✅ 已修复：范围页停留 23 秒仅 1 次请求（此前约 45 次/秒）。
- ~~**修 DEF-20260904-011（P1）**~~ ✅ 已修复：范围表格已渲染，批准/拒绝可用，进度可达 100%，主链首次越过第 2 阶段。

### 现在需要你提供的（按优先级）

1. **A1**：给一个有余额的 DeepSeek（或其他官方模板提供方）API Key。
   **这一条现在真的成了首要阻断**——阶段 2 已通，契约/场景/执行都需要 AI。
   好消息：平台已新增「官方一键模板」（DeepSeek/GLM/GPT/Qwen），选模板后 base_url 与全量模型清单自动预置，
   你只要粘 Key 即可，不用再手填地址和模型名。注意 Key 按项目隔离且编辑时不回显，请直接给本项目（id=13）配置。
2. **A2**：给一个能真正登录、且有业务数据的账号密码（解锁收藏/聊天/银钻/商城/装扮/付费约 43 条用例）。
3. **B1**：回一句「可以启动 runner」，我立刻按 Batch 206 §4 配方打通 Test5 执行链
   （环境侧 `runner_key=test5-internal-01` 我已配好，新项目 `PROJECT_ID=13` 我也已确认，随时可启）。
4. **A3**：给一份可导入的 OpenAPI JSON/YAML。
   这条的优先级比昨天高了：契约阶段现在能生成，但**生成出来是空壳**（只有 `content_hash`，无任何字段/标准答案，
   见 DEF-20260905-001）。没有接口契约来源，下游场景与断言就无据可依。

### 建议平台侧接着修的（不需要你提供资料）

复测新录入 9 条 DEF-20260905-001..009，其中 3 条 P1 建议优先：

- **DEF-20260905-001（P1）**：生成契约只产出元数据，空壳契约仍可「冻结」。
- **DEF-20260905-002（P1）**：版本验收任务创建后在界面上不可达（无列表页 + 侧边栏死链），只能手打 `/version-tasks/{id}`。
- **DEF-20260905-003（P1）**：「一键运行」`blocked/total=0` 空跑，界面却显示「已执行 / 阻塞 0」，无原因说明。

另外建议把 **DEF-20260904-008 升级为 P1**：复测发现 DEF-20260904-004…-015 共 12 个编号在两个项目里指向完全不同的缺陷，
其中就包括本次的 P0（-010）与两条 P1（-011、-012）——按编号沟通会拿到错误缺陷。

B2、B3、C1–C10 不阻塞我开始跑链路，可在过程中并行补齐。

